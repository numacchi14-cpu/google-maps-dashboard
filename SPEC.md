# Google Maps マイプレイス ダッシュボード 仕様書

最終更新: 2026-07-21
現状のコード（`index.html` / `app.js` / `style.css`）を読み込んで作成。
これまで Antigravity（Gemini）で開発 → Claude へ引き継ぎ検討中。

---

## 1. 概要

Google Takeout でエクスポートした「保存済みの場所」「クチコミ（レビュー）」データを
ブラウザ上に読み込み、都道府県別・カテゴリー別に可視化するダッシュボード。

- **完全クライアントサイド処理**：アップロードされたデータはサーバーに送信されず、ブラウザ内（JSメモリ上）のみで処理される。バックエンドなし、DBなし。
- **単一ページアプリ**：`index.html` + `app.js`（約1650行）+ `style.css` の3ファイル構成。
- 外部ライブラリはすべてCDN経由（Leaflet, Chart.js, Lucide Icons, Google Fonts）。ローカル同梱なし。

## 2. 主な機能

### 2.1 データ取り込み
- 対応形式: `.json`（Saved places / Reviews）, `.geojson`, `.csv`
- ドラッグ＆ドロップ／ファイル選択の両方に対応、複数ファイル同時取り込み可
- 「サンプルデータをロード」ボタンで15件のダミーデータを試せる
- パーサーはフォーマットゆらぎに強い「ロバスト抽出」方式：
  - 名前・住所・URL・コメント・評価・投稿日／更新日を、複数パターンのキー名（日本語/英語、ネスト構造含む）から総当たりで探索
  - Saved Places GeoJSON、Reviews JSON（配列 / `{reviews:[...]}` ラップ両対応）、CSV（ヘッダー名から列を推定）をそれぞれ専用パーサーで処理
- 取り込み後、URL／座標／名前+住所をキーに重複排除（`deduplicatePlaces`）。重複時はコメントや評価が空の方を埋め合わせてマージ。マイ都道府県／マイカテゴリー（後述）は再インポートで上書きされず、未設定の場合のみ埋め合わせる
- **クチコミの手動追加・編集**: Takeoutのエクスポート漏れ（600件上限等）を補うためのモーダルフォーム。スポット名のみ必須、住所・評価・コメント・投稿日・Googleマップリンク・緯度経度（`35.6586, 139.7454`形式のペースト対応）は任意。既存の手動入力エントリは一覧の編集アイコンから再編集可能
- **手動入力データのCSV一括編集**: 手動入力（`source: "手動入力"`）のみを対象にCSVエクスポート／インポート。IDが一致する行は上書き、不一致・空欄は新規追加として扱う

### 2.2 データ加工・分類
- **都道府県の自動判定**（`extractPrefecture`）: 住所→都道府県名の直接一致 → 英語県名 → 主要都市名からの推測 → 座標から最も近い県庁所在地への距離計算、の順にフォールバック
- **カテゴリー自動分類**（`classifyCategory`）: 12カテゴリー（グルメ6種＋観光/寺社仏閣/宿泊/ショッピング/交通機関/その他）に、店名・コメントの正規表現キーワードマッチで分類。誤判定防止のための除外パターン（例:「イン」で終わる語からホテルを判定するが「ワイン」「デザイン」等は除外）も作り込まれている。取り込み時点ではこのヒューリスティックが常に`category`の初期値になる
- **Google連動カテゴリーの実データ取得（Geminiプロンプト方式）**（2026-07-21実装）: ヘッダーの「カテゴリーをGeminiで取得」ボタン→モーダルで、①スポット名・住所のみ（評価・レビュー本文は含めない）を含むプロンプトを生成→②ユーザーがGeminiにコピー＆貼り付けして実行→③返ってきたJSON配列（`[{"id":..,"category":".."}]`）をモーダルに貼り戻して「適用する」、の3ステップ。差分方式で、`googleCategoryRaw`（Geminiが返した生の業種ラベル）が未設定のスポットのみを対象にし、1バッチ最大50件（`GEMINI_CATEGORY_BATCH_SIZE`）ずつ処理、適用後は自動的に残りの未取得分の次バッチプロンプトに切り替わる。適用時、生ラベルは`googleCategoryRaw`にそのまま保持しつつ、`mapRawCategoryToKey`（`CATEGORIES`各カテゴリーの`keywords`を流用した軽量マッチング）で12カテゴリーへマッピングして`category`を更新する。生データを保持するため、将来分類ロジックを見直しても再取得なしで再マッピング可能。JSONバックアップのエクスポート/インポートは`googleCategoryRaw`を保持するが、CSVは他のマイカテゴリー同様に生データを保持しない（4節）。Google Places API方式（実データ取得のもう一つの選択肢）は未着手
- **マイ都道府県／マイカテゴリー（2軸分離）**: Google連動側（自動判定結果）とは別軸で、各行を表内のドロップダウンから手動上書き可能。上書きした値（`myPrefecture`/`myCategory`）は再インポート時も保護される
- **マイカテゴリーの管理**: 標準12カテゴリーに加えて、ユーザーが独自カテゴリー（名前・色）を作成・削除できる管理モーダル（`registerCustomCategory`/`deleteCustomCategory`）。作成したカテゴリーは各行のマイカテゴリー欄で選択可能

### 2.3 表示・操作
- **統計カード**: 総スポット数／制覇した都道府県数／最多カテゴリー
- **一覧テーブル**: 名前・都道府県・カテゴリー・住所（横スクロール対応）・評価（★）・レビュー本文・最終更新日・操作（地図中心表示／削除、手動入力エントリのみ編集も可）。列ヘッダークリックでソート
- **検索・フィルター**: 名前/住所/コメントの部分一致検索、都道府県・カテゴリー・評価・最終更新日の範囲での絞り込み（件数付きドロップダウン）
- **インタラクティブマップ**（Leaflet、ダークタイル）: フィルター結果を円マーカーで表示、クリックでポップアップ（名前・県・カテゴリー・評価・コメント・Googleマップリンク）
- **グラフ**（Chart.js）: カテゴリー比率（ドーナツ）、都道府県別件数トップ10（横棒）
- **エクスポート**: CSV（BOM付きUTF-8、Excel対応）／JSON、いずれもダウンロード
  - **使い分けの方針**：**JSON＝完全なバックアップ・復元用**（`categoryKey`/`myCategoryKey`/`myCategoryName`/`myCategoryColor`等キー情報ごと保持するため、手動入力の`source`やマイカテゴリー・マイ都道府県、未登録の独自マイカテゴリーまで含めて一切失わずに再読み込みできる）。**CSV＝Excelで見る・一括編集するための出力**（カテゴリー等は表示名で書き出すため人間が読みやすい一方、そのカテゴリーがまだ一度も作られていない状態での復元はできない、という構造的な制約がある。詳細は4節）。データを完全に保全したい場面（バックアップ、機種変更、リセット前退避）は必ずJSONを使うこと
- **リセット**: 確認ダイアログ後、全データ・フィルター・マップをクリア

### 2.4 UI/UX
- ダークテーマ、ガラスモーフィズム調のカード、グラデーションアクセント
- レスポンシブレイアウト対応（768px以下は一覧テーブルをカード表示に切り替え）

## 3. データモデル（1件あたり）

```js
{
  id, name, address, lat, lng,
  prefecture, category,          // Google連動軸（自動判定結果。生データがあればmapRawCategoryToKeyでの再マッピング結果）
  googleCategoryRaw,             // Gemini/Places API等から取得した生の業種ラベル（未取得はnull。12種への正規化前の情報を保持）
  myPrefecture, myCategory,      // マイ都道府県／マイカテゴリー軸（手動上書き。未設定はnull）
  rating (1-5 or null),
  comment, url, source,          // "保存済みの場所" / "クチコミ投稿" / "CSVインポート" / "サンプルデータ" / "手動入力"
  publishTime, updateTime        // "YYYY/MM/DD" 文字列
}
```

表示・フィルター・ソートは常に `getEffectivePrefecture`/`getEffectiveCategory`（`myPrefecture || prefecture` 等）経由で「実効値」を参照する。

## 4. 既知の制約・設計上の注意点

- **状態はメモリ上のみ**：リロードで全データが消える（永続化なし、localStorage等未使用）
- **外部CDN依存**：オフライン環境や配布用アプリ化の際は、Leaflet/Chart.js/Lucide/フォントをローカル同梱する必要あり
- **カテゴリー分類は正規表現ベースのヒューリスティック**：厳密なNLPではないため、新しい店名パターンに弱い可能性がある（手動編集で救済可能な設計にはなっている）
- **重複排除ロジック**は完全ではない（URL/座標/名前ベースの近似マッチ）
- **CSVは完全な無劣化バックアップ形式ではない**：メイン画面のCSVエクスポート（`exportCSV`）はカテゴリー・マイカテゴリーを内部キーではなく表示名で書き出す（Excelでの可読性を優先した設計）。再インポート時（`parseAppCSVBackup`）は表示名→キーの逆引きで復元するため、**既に登録済み／標準12種のカテゴリーは問題なく復元できる**が、**まだ一度も作成していない独自マイカテゴリー名だけは色情報が無く復元できず、その行は上書きなしにフォールバックする**。同様に、Geminiで取得した`googleCategoryRaw`（生の業種ラベル）もCSVには列として出力していないため、CSVだけを介した再読み込みでは失われる（既存の在メモリレコードとの再統合時は埋め合わせで保護されるが、ブラウザ再読み込み後にCSVのみから復元する場合は再取得が必要）。完全に無劣化でバックアップ・復元したい場合は必ずJSONエクスポートを使うこと（`parseAppBackupJSON`は`categoryKey`・`googleCategoryRaw`等キー情報ごと保持するためこの制約がない）
- テストは `tests/`（Node標準 `node --test`、`npm test`で実行）に `classifyCategory`／`parseAppBackupJSON`／`buildManualPlaceFields`／`matchesDateRange`／`parseCoordinatePair`／`parseCSVRows`／`parseAppCSVBackup`／`mapRawCategoryToKey`・`buildGeminiCategoryPrompt`・`parseGeminiCategoryResponse`・`applyGeminiCategoryResults`（`tests/geminiCategory.test.js`）の分が存在（2026-07-20〜21）。それ以外のロジックはまだ未カバー
- READMEはまだ存在しない

## 5. 今後の要望・ロードマップ

*（ここにやりたいことを追記していく欄）*

### アプリ化・アクセス性
- [ ] Windows / スマホで「ブラウザを経由せず起動できる」形にしたい → PWA化を検討中（マニフェスト＋Service Worker追加、既存コードはほぼ無改修で可）
- [ ] Googleアカウント連動などで、スマホでもPCでもアクセスできるようにしたい

**方針: 保存先はGoogleドライブにする**
- 自前サーバー・DBを持たず、Drive REST APIをブラウザから直接呼ぶ形にすることで「バックエンドなし」の設計思想を維持できる
- OAuthスコープは `drive.file`（アプリが作成したファイルのみアクセス可）に限定し、Drive全体への権限は要求しない
- Google Cloud ConsoleでのOAuthクライアント登録は必要だが、個人利用（自分のみ使用）なら「テストモード」のままでよく、審査・追加費用は不要
- オフライン時にも使えるよう、ローカル（IndexedDB等）に直近データをキャッシュしつつ、開いたタイミングでDriveの最新版を取得する二層構成にする（PWA化と相性が良い）
  - **注意点**：ローカルキャッシュは暗号化されず端末上に平文で残るため、共有PC等では望ましくない場合がある
  - **対応方針**：設定画面に「このデバイスにデータを保存する」トグルを用意し、**端末ごとに**キャッシュのON/OFFを選べるようにする（例：スマホはON＝オフラインでも閲覧可、共有PCはOFF＝毎回Driveから取得しローカルに残さない）。ローカルキャッシュはそもそも端末ごとに独立しているため、端末間の連携は不要でシンプルに実現できる
- 複数端末での同時編集は想定しにくいため、競合解決は「開いたら毎回Driveの最新を取得して上書き」というシンプルな方式で十分（過剰な差分マージ機構は作らない）
- [ ] 個人情報の取り扱いに配慮したい（現状すでにクライアントサイド完結・サーバー送信なしの設計は維持する方針。Drive連携後も「自分のGoogleアカウントの中で完結」という原則を保つ）

### カテゴリー機能の強化

**方針: カテゴリー・都道府県ともに2軸に分離する**
- **Google連動カテゴリー**: Googleマップ側の業種情報を反映する軸。取得方法は以下2通りを選択式にする
  - a) Google Places API等で実データを取得（正確だが要課金）→ 未着手
  - b) Geminiプロンプト方式：アプリ側で「スポット名・住所のみ」を整形してプロンプト化し、ユーザーがGeminiに貼り付けて分類結果を取得、アプリに貼り戻す（追加課金なし、既存Geminiアカウントの範囲で完結）→ **実装済み（2026-07-21）**。詳細は2.2節
    - **プライバシー配慮**：評価・レビュー本文はプロンプトに含めない。名前と住所のみを渡すことで、個人的な感想・コメントの外部送信を避ける
    - **差分方式**：毎回全件ではなく「Google連動カテゴリーが未設定のスポットのみ」を対象にする。プロンプトの肥大化と、送信範囲の無駄な拡大の両方を防ぐ
    - **注意点（バッチ分割）**：差分方式でも、初回インポート時など未分類が数百件単位になる場合は1回のプロンプトに収まらない可能性があり、バッチ分割の仕組みが必要になる → 1バッチ50件で分割、適用後は自動的に次バッチへ進む形で対応
    - **注意点（出力フォーマットの固定）**：Geminiの返答が毎回同じJSON構造で返るよう、プロンプト側で厳密に指定する必要がある。ブレるとアプリ側のパースが失敗する → ```json フェンス付き回答は許容、配列以外/壊れたJSONはエラー表示にフォールバック
    - **注意点（手動ステップが挟まる）**：API方式と違い完全自動ではなく、「プロンプトをコピー→Geminiで実行→結果を貼り戻す」という操作が毎回必要になる
  - どちらの方式で取得しても、誤判定はあり得るため**手動で上書き修正可能**にする（既存のマイカテゴリー上書き機能でカバー）
  - Googleの実際の業種タグは12カテゴリーよりずっと細かいため、**取得した生データはそのまま保持**し（`googleCategoryRaw`）、一覧・グラフ表示時にのみ12カテゴリーへマッピングする（正規化時に情報を失わないようにする）
- **マイカテゴリー / マイ都道府県**: ユーザーが自作する独自タクソノミー軸。常にユーザー所有で、インポート処理は一切触らない
  - 例：Google連動カテゴリーは「ラーメン店」のままでも、マイカテゴリーで「よく行く店」のように独自に管理できる
  - 都道府県も同じ理由（再インポートで上書きされたくない）が当てはまるため、カテゴリーと同じ2軸構成にする
- [x] 自分でカテゴリーを作成・登録できるようにしたい（2026-07-20実装）
  - マイカテゴリー軸（`myCategory`）として実装。「マイカテゴリー設定」モーダルから名前・色を指定して作成・削除できる（`customCategories`、`registerCustomCategory`/`deleteCustomCategory`）
  - Google連動カテゴリーは引き続き `CATEGORIES` 定数の12種類固定（実データ取得のa/b方式は未着手、下記）
- [x] Google連動カテゴリーの実データ取得（Geminiプロンプト方式）を実装したい（2026-07-21実装）
  - ヘッダーの「カテゴリーをGeminiで取得」ボタンからモーダルを開き、①プロンプト生成＆コピー→②Geminiで実行→③回答JSONを貼り付けて適用、の3ステップで反映（詳細は2.2節）
  - `googleCategoryRaw`（未取得はnull）に生の業種ラベルを保持しつつ、`mapRawCategoryToKey`で12カテゴリーへマッピングして`category`を更新。差分方式・50件バッチで実装
  - JSON バックアップは`googleCategoryRaw`を含めて完全復元できるが、CSV往復では失われる（4節のマイカテゴリー名と同じ制約のクラス）
  - 残: Google Places API方式（選択肢a）は未着手。優先度は低い（Geminiプロンプト方式で当面の要望は満たせるため）
- [x] 定期的にGoogle Takeoutからデータを再読み込みしても、自分で編集したカテゴリー（や都道府県）が上書きされず維持されるようにしたい（2026-07-20実装）
  - マイ都道府県／マイカテゴリー軸の追加により構造的に解決：`deduplicatePlaces` は再インポート時、既存レコードの `myPrefecture`/`myCategory` が未設定の場合のみ埋め合わせ、既存の手動編集値は上書きしない
  - 一覧の都道府県／カテゴリー列は `getEffectivePrefecture`/`getEffectiveCategory`（マイ軸があればそちらを優先）で表示・フィルター・グラフ集計する
  - 残課題：重複排除ロジック自体の精度（URL/座標/名前ベースの近似マッチ）が引き続き前提となる。同じ場所が「別データ」と誤判定されると、せっかく守った手動編集が引き継がれない

### データ入出力
- [ ] Googleドライブからダウンロードしたjsonを読み込めるようにしたい
- [ ] 毎回ファイルを読み込まなくても、前回読み込んだデータをそのまま見られるようにしたい（永続化。現状はメモリ上のみでリロードすると消える）→ 上記のGoogleドライブ保存先と合わせて実現
- [x] アプリ内にGoogle Takeoutへ直接アクセスできるボタン（リンク）を新設する（2026-07-20実装）
  - アップロードエリアに「Google Takeoutでデータを取得する」リンクを追加。新しいタブで `https://takeout.google.com/settings/takeout` を開く

### 一覧・操作性
- [x] 整理した一覧から、修正したいクチコミを選んだら地図上で確認できるようにしたい（2026-07-20実装）
  - **方針を修正**：Googleマップへの外部遷移ではなく、一覧の「操作」列の地図ピンアイコンをクリックした時に、地図の中心移動・ポップアップ表示に加えて**インタラクティブマップの位置までスムーズスクロール**するようにした（テーブルが上・マップが下のレイアウトのため）
- [x] Takeoutでエクスポートされない／漏れるクチコミを手動で追加・編集できるようにしたい（2026-07-20実装）
  - 「クチコミを手動で追加」モーダル（`addManualPlace`/`updateManualPlaceFields`）。緯度経度は`35.6586, 139.7454`形式のペーストに対応（`parseCoordinatePair`）、住所から都道府県・カテゴリーは通常のインポートと同じロジックで自動判定
  - 手動入力エントリのみ一覧に編集アイコンを表示し、モーダルを再オープンして再編集可能。まとめて直したい場合向けにCSVエクスポート／インポートにも対応（`exportManualCSV`/`importManualCSV`、IDが一致する行は上書き）
  - **付随して見つかった不具合を修正**：地図中心表示ボタンを追加した際に重複した`class`属性（`class="btn" ... class="btn-locate"`）とセレクタの曖昧さ（`querySelector("button")`が最初の1つしか拾わない）が原因で、編集ボタン追加後に地図中心表示が壊れる可能性があった。専用クラス（`.btn-locate`/`.btn-edit-manual`）で確実に紐付くよう修正し、手動追加→編集→保存後も地図ボタンが正しく機能することを確認済み
- [x] 最終更新日で範囲指定して絞り込みたい（2026-07-20実装）
  - `filter-date-from`/`filter-date-to`（`<input type="date">`）と`matchesDateRange`関数で実装。片方のみ指定でも以降/以前で絞り込み可能

### モバイルレイアウトの作り込み
- [x] スポット一覧テーブル（8列）のスマホ向けレイアウトを設計する（2026-07-20実装）
  - 768px以下では、テーブルではなく「1件＝1枚のカード」形式（ラベル＋値を縦積み、スポット名がカードタイトル）に切り替え。横スクロール依存を解消し、住所・レビューは折り返し表示に変更
  - 地図・グラフ部分（3カラム→1カラム化）は既存対応のままで問題なし

### 運用・品質面での提案（Claudeより）
- [x] リセット操作の前に自動でJSONバックアップを促す導線を追加する（誤操作で全データを失わないように）（2026-07-20実装）
  - `resetApp()` でデータが1件以上ある場合、リセット確認の前に「先にJSONバックアップをダウンロードしますか？」の確認を追加
  - **付随して見つかった不具合を修正**：エクスポートしたJSONを再アップロードしても、座標（キー名不一致で `coordinates.latitude/longitude` が読めていなかった）と手動編集したカテゴリー・都道府県（常に自動判定で再計算されていた）が復元されないバグがあった。専用の復元パーサー（`parseAppBackupJSON`、`categoryKey` の有無で自社エクスポート形式を検出）を追加し、保存済みの値をそのまま復元するように修正
- [x] `classifyCategory` の分類ロジックに対して、代表的なケース（ラーメン店、ホテル「〜イン」の誤判定除外など）の簡単なテストを用意する（2026-07-20実装）
  - `tests/classifyCategory.test.js`、`tests/parseAppBackupJSON.test.js` を追加。以降 `buildManualPlaceFields.test.js`／`matchesDateRange.test.js`／`parseCoordinatePair.test.js`／`parseCSVRows.test.js`／`parseAppCSVBackup.test.js` も追加（Node標準の `node --test`、追加npm依存なし）。`npm test` で実行可能
- [x] CSVインポートが「ぐるぐる回ったまま固まって戻ってこない」不具合を修正（2026-07-20実装）
  - 原因：クチコミ本文が複数行（引用符内改行）にまたがるCSVで、行を先に改行分割してから解析していたためレコードが分裂し `undefined` が混入 → 重複排除処理が例外を投げる → ローディング画面を閉じる処理まで届かず無限にスピナーが回っていた
  - CSV全体を引用符を認識しながら一括パースする方式（`parseCSVRows`）に変更し、複数行のレビュー本文も1レコードとして正しく読み込むように修正。列が足りない行は`undefined`ではなく空文字にフォールバック（`csvField`）。`handleFiles`本体を`try/finally`で保護し、想定外のエラーが起きても必ずローディング画面が閉じるようにした
- [x] CSVエクスポートを最初のアップロード画面から再読み込みすると、手動入力エントリの編集アイコンが消える（マイカテゴリー等の上書きも失われる）不具合を修正（2026-07-20実装）
  - 原因：`parseCSVData`（汎用CSV取り込み）が「データソース」列を一切読んでおらず、再インポートしたレコードは常に`source: "CSVインポート"`として扱われていた
  - JSONバックアップ復元と同じ考え方で、CSVヘッダーの「データソース」列の有無から自社の全件エクスポート形式を検出し、専用の復元処理（`parseAppCSVBackup`）で`source`・都道府県・マイ都道府県・カテゴリー・マイカテゴリーをそのまま復元するように修正。カテゴリー名→キーの逆引きで変換するため、未登録の独自マイカテゴリーだけは復元できない制約が残る（4節に記載）

## 6. 実装の進め方（フェーズ）

**方針：機能の作りこみを先に、PWA化（特にオフライン対応部分）は後回しにする**

- PWA化はマニフェスト＋Service Workerを既存コードにほぼ手を入れず後付けできるため、先にやる必然性が薄い
- オフラインキャッシュの設計（端末ごとのON/OFF等）はGoogle Drive連携（認証・同期方式）が固まって初めて具体化するため、Drive連携より先にやると二度手間になりやすい
- 「ブラウザUIなしで単体ウィンドウ起動する」体験だけ早めに試したい場合、Service Workerなしでマニフェストだけ先に用意するのはコストが低く問題ない

| フェーズ | 内容 | 状況 |
|---|---|---|
| 1 | 認証・同期に依存しないロジック追加（マイカテゴリー/マイ都道府県の2軸分離、Geminiプロンプト機能、Takeoutボタン、クチコミ→Googleマップ遷移、リセット前バックアップ、クチコミ手動追加・編集、日付範囲フィルター、分類ロジックのテスト、モバイル一覧のカード化） | Takeoutボタン／クチコミ地図遷移／リセット前バックアップ（＋復元バグ修正）／分類テスト／モバイルカード化／マイカテゴリー・マイ都道府県2軸分離／クチコミ手動追加・編集（CSV一括編集込み）／日付範囲フィルター／Google連動カテゴリーのGeminiプロンプト方式取得は完了（2026-07-20〜21）。残: Google連動カテゴリーのGoogle Places API方式（bの代替案a、未着手・優先度低） |
| 2 | Google Drive連携（OAuth、保存・読み込み） | 未着手 |
| 3 | オフラインキャッシュ設計（IndexedDB、端末ごとのON/OFF設定） | 未着手 |
| 4 | PWA本実装（マニフェスト＋Service Worker、オフライン対応込み） | 未着手 |

<!-- 以下、追加の要望をここに書き足していってください -->
