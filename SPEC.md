# Spotlog（旧称: Google Maps マイプレイス ダッシュボード）仕様書

最終更新: 2026-07-28
現状のコード（`index.html` / `app.js` / `style.css`）を読み込んで作成。
これまで Antigravity（Gemini）で開発 → Claude へ引き継ぎ検討中。

---

## 1. 概要

Google Takeout でエクスポートした「保存済みの場所」「クチコミ（レビュー）」データを
ブラウザ上に読み込み、都道府県別・カテゴリー別に可視化するダッシュボード。

- **完全クライアントサイド処理**：アップロードされたデータはサーバーに送信されず、ブラウザ内（JSメモリ上）のみで処理される。バックエンドなし、DBなし。
- **単一ページアプリ**：`index.html` + `app.js`（約1650行）+ `style.css` の3ファイル構成。
- 外部ライブラリはすべてCDN経由（Leaflet, Chart.js, Lucide Icons, Google Fonts）。ローカル同梱なし。

## 2. 主な機能

### 2.1 データ取り込み
- 対応形式: `.json`（Saved places / Reviews）, `.geojson`, `.csv`
- ドラッグ＆ドロップ／ファイル選択の両方に対応、複数ファイル同時取り込み可
- 「サンプルデータをロード」ボタンで15件のダミーデータを試せる
- **ダッシュボード表示中の追加インポート導線（2026-07-24修正）**：ドロップゾーン（`#drop-zone`）は初回アップロード画面（`#upload-section`）にしかなく、データ読み込み後はその画面ごと非表示になるため、**新しいTakeoutエクスポートを追記していく**というこのアプリの中核的な使い方（2.1節後述の累積方針）にもかかわらず、読み込み後は追加インポートの手段が画面上から消えてしまっていた（リセットしないと再度ファイルを選べない状態）。ヘッダーに「ファイルを追加読み込み」ボタンを追加し、非表示状態の`#file-input`を`.click()`する形で解消（`display:none`な祖先を持つファイル入力でも`.click()`によるファイル選択ダイアログの起動は問題なく動作することをブラウザ実機で確認済み）
- **一覧画面（ダッシュボード表示中）からもGoogle Takeoutへ直接遷移できるボタンを追加（2026-07-26実装）**：アップロード前の画面には元々「Google Takeoutでデータを取得する」リンクがあったが、データ読み込み後の画面（`#header-actions`）には無く、追加のTakeoutエクスポートを取りに行きたいときに一度リセットするか記憶を頼りにURLへ直接アクセスする必要があった。ヘッダーの「ファイルを追加読み込み」ボタンの隣に「Google Takeoutを開く」リンクを追加し、新しいタブで`https://takeout.google.com/settings/takeout`を開けるようにした
- パーサーはフォーマットゆらぎに強い「ロバスト抽出」方式：
  - 名前・住所・URL・コメント・評価・投稿日／更新日を、複数パターンのキー名（日本語/英語、ネスト構造含む）から総当たりで探索
  - Saved Places GeoJSON、Reviews JSON（配列 / `{reviews:[...]}` ラップ両対応）、CSV（ヘッダー名から列を推定）をそれぞれ専用パーサーで処理
- 取り込み後、URL／座標／名前+住所をキーに重複排除（`deduplicatePlaces`）。マイ都道府県／マイカテゴリー（後述）は再インポートで上書きされず、未設定の場合のみ埋め合わせる
  - **重複時のマージ方針（2026-07-22実装、2026-07-26に判定基準を修正）**：Googleマップ側でレビュー本文・評価を編集すると、Takeout再エクスポート時に「最終更新日」が更新されて返ってくる。この性質を利用し、既存レコードより取り込みデータの最終更新日が新しい場合は、コメント・評価・住所・都道府県・最終更新日・Google連動カテゴリー（`googleCategoryRaw`/`category`）を新データで**追従上書き**する。取り込みデータの方が同じか古い場合は、従来通り「既存が空欄の項目だけ埋める」方式に留める。**手動入力**（`source: "手動入力"`）レコードはこの追従上書きの対象外（ユーザーが直接編集したデータのため）。マイ都道府県／マイカテゴリーは最終更新日に関わらず常に「未設定の場合のみ埋め合わせ」で、ユーザーの手動上書きを保護する。**新旧判定に使うフィールドは内部的には`publishTime`**（キー名に反して実質「最終更新日」として使っている。2026-07-22時点では`updateTime`で判定していたが、実データではこのフィールドがほぼ常に空で機能していなかったため2026-07-26に修正。詳細は4節）
  - 背景：Google Takeoutは直近600件程度までしかエクスポートできない制限があるため、このアプリでデータを蓄積し続けること自体に価値がある。単純な「新データで全置換」ではなく「追加＋条件付き追従マージ」にすることで、蓄積（600件を超えた古いデータの保持）とGoogle側の編集追従を両立させている
- **削除済みスポット（ゴミ箱）と再インポート時の自動再登録防止（2026-07-26実装）**：一覧の削除ボタン／後述の「不明なスポットの確認」からの削除は、即座に消去せず`deletedPlaces`（ゴミ箱）へ移す方式に変更。従来は削除しても内部的にはIDを消すだけで「削除済み」という記録が一切残らなかったため、Google Takeoutを再エクスポート・再インポートすると（Takeout側は現在保存中/投稿中の全件を毎回書き出すため）同じスポットがそのまま復活してしまう問題があった（ユーザー報告により発覚）。ゴミ箱に入っている間は、重複排除と同じマッチキー（`buildPlaceMatchKey`：URL→名前+緯度経度→名前+住所の3段階、`deduplicatePlaces`と共通化）を使って`handleFiles`の取り込み対象から除外することで、再インポートしても復活しないようにしている。ヘッダーの「削除済みスポット」ボタン（未読み込み件数をバッジ表示）からゴミ箱の中身を確認でき、「復元する」で一覧に戻す、「完全に削除」でゴミ箱から消して以後の再インポートでの復活を許可する、のどちらかを選べる（誤削除への保険という位置づけ、ユーザー要望により追加）
  - **永続化はローカルキャッシュ（IndexedDB）に連動**：ゴミ箱の中身は氏名・住所を含む実データのため、`places`本体と同じIndexedDBレコードに`deletedPayload`として保存し、「このデバイスにデータを保存」トグルのON/OFF・`resetApp()`・トグルOFF時の即時削除といった既存のプライバシー方針（4節）にそのまま従う（トグルOFF時・リセット時はゴミ箱も同時に空にする）。JSON/CSVエクスポートやDrive保存の対象には含めない（Drive/JSONバックアップは常に「現在の`places`」を保存する設計であり、そこには元々削除済みスポットが含まれないため、この機能のためだけに別途持ち出す必要がない）
- **不明なスポットの確認（2026-07-26実装）**：Takeout・GeoJSON・汎用CSVのロバスト抽出パーサー（`extractNameRobustly`）は、スポット名が一切見つからない場合に名前を`"不明なスポット"`として埋めるが、これは主に「閉店・削除済みでGoogle側の情報が失われたスポット」で発生する。取り込み直後、`places`内に名前が`"不明なスポット"`の項目が見つかった場合は「不明なスポットの確認」モーダルを自動的に開き、住所・都道府県・評価などを手がかりに要不要をチェックボックスで選んで削除できるようにした（削除した場合は上記のゴミ箱に入る）。何もしなければ一覧にそのまま残る
- **ログの手動追加・編集**（ボタン名は「ログを手動で追加」。旧名称「クチコミを手動で追加」、2026-07-26改名。理由は4節参照）: Takeoutのエクスポート漏れ（600件上限等）を補うためのモーダルフォーム。スポット名のみ必須、住所・評価・コメント・投稿日・Googleマップリンク・緯度経度（`35.6586, 139.7454`形式のペースト対応）は任意。既存の手動入力エントリは一覧の編集アイコンから再編集可能
- **手動入力データのCSV一括編集**: 手動入力（`source: "手動入力"`）のみを対象にCSVエクスポート／インポート。IDが一致する行は上書き、不一致・空欄は新規追加として扱う
- **スポット情報のGemini検索（手動追加の補助、2026-07-24実装。ボタン名は2026-07-26に「スポット情報をGeminiで調べる」へ改名）**: 600件上限などで漏れた古いクチコミを手動追加する際、正式名称・住所・緯度経度・カテゴリーの手入力が大変という声を受けて追加。ヘッダーの「スポット情報をGeminiで調べる」ボタン→モーダルで、①うろ覚えの手がかり（`都道府県 店名`のような自由記述、1行1件、最大30件）を入力→②プロンプトを生成してコピー→③Geminiに貼り付けて実行→④返ってきたJSON（`[{"id":"q1","candidates":[{"name":..,"address":..,"lat":..,"lng":..,"category":..}]}]`形式）を貼り戻す、の4ステップ
  - 既存のGoogle連動カテゴリー取得（Geminiプロンプト方式）と同じ「プロンプト生成→貼り付け→適用」の作法だが、対象が**既存のplacesではなくまだ登録されていない候補**である点が異なるため、候補が複数返ってきた場合はカードから選ぶ選択式UIを挟む（0件なら「ログを手動で追加」への案内、1件でも自動確定はせず確認ボタンを挟む）
  - 選んだ候補は`手動入力`レコードとして即時追加。業種は12種への丸め込みをせず既存のGemini取得カテゴリーの仕組み（`googleCategoryRaw`/`getOrCreateGeminiCategory`）にそのまま乗せる。**コメント・投稿日はGeminiには分からないため空欄のまま**追加し、後から一覧の編集アイコンで書き足す運用（感想は後入力でよい、とユーザーと合意）
  - **候補カードでの評価選択（2026-07-25実装）**：評価だけは候補カードに★1〜5のドロップダウン（未選択可）を添えて、「これを追加する」を押した時点でまとめて設定できるようにした。行った先で「良かった/普通だった」くらいの評価は覚えている一方、コメント入力までは後回しにしたい、という運用に合わせたもの。未選択のまま追加した場合は従来通り`rating: null`（`buildManualPlaceFieldsFromLookupCandidate`の第2引数）
  - **ハルシネーション対策**：緯度経度はGeminiが不正確な値を返すリスクが業種判定より高いため、候補の住所から判定した都道府県と、座標から最寄りと判定した都道府県が食い違う場合に候補カード上へ警告バッジを表示する（`checkPlaceLookupCoordinateMismatch`。ブロックはせず警告のみ、既存の`PREFECTURE_COORDINATES`による最近傍判定を流用）
  - 評価・レビュー本文を送信しない設計は既存のGoogle連動カテゴリー取得機能と同様（プライバシー配慮）
- **Googleマップのカスタムリスト（「行ってみたい」等）CSVの取り込み（2026-07-28実装）**：Google Takeoutの「保存済みの場所」フォルダには、`Saved places.json`（保存済みの場所全体）とは別に、ユーザーが作成したカスタムリストが**リスト名そのままのファイル名のCSV**として個別に含まれる（例: `行ってみたい.csv`）。このCSVはヘッダーが`タイトル,メモ,URL,タグ,コメント`固定で、住所・緯度経度・評価は一切含まれない、既存の「保存済みの場所／クチコミ」とは全く異なるフォーマット。汎用CSVパーサー（`parseCSVData`）の名前列判定は英語"title"のみを見ており「タイトル」という表記にはマッチしないため、従来はこのCSVを読み込んでも1件も取り込めなかった（全行が「名称未設定」として捨てられていた）
  - **専用パーサー**：`isSavedListCSV`（ヘッダーに「タイトル」「URL」が両方あり、住所・緯度・経度列が無いことで判定）→`parseSavedListCSV`（ファイル名を`listName`として渡す）を`handleFiles`のCSV分岐に追加。住所・緯度経度が無いため都道府県は「その他・海外」・カテゴリーは店名のみのヒューリスティック判定になるが、以降は既存の重複排除・保存フローにそのまま乗る
  - **完全に別管理ではなく、既存の`places`に混在させて絞り込みで分ける方式を採用（ユーザーと相談の上で決定）**：当初は別データ・別セクション管理を検討したが、①同じURLの店が「行った（クチコミ済み）」と「行ってみたい」の両方にまたがっていても既存の重複排除ロジック（`buildPlaceMatchKey`：URL優先）が安全にマージするだけで壊れないこと、②そのマージによって「行きたいと思っていた店に実際行った」を検出できる副次的な価値があること、から方針転換。新しい永続化経路（Drive/ローカルキャッシュの二重管理）を作らずに済むメリットも大きい
  - **新フィールド**：`wishlistListName`／`wishlistMemo`／`wishlistTags`／`wishlistComment`（すべて任意、3節参照）。既存の`comment`（クチコミ本文）とは別物として扱う（同じ場所が既にクチコミ登録済みの場合に上書きしてしまわないため）。`deduplicatePlaces`では、マイ都道府県／マイカテゴリーと同じく「既存が空欄の場合のみ埋め合わせ」で保護するため、行ってみたいで先に取り込んだ後にクチコミが来ても、逆順でも、両方の情報が1レコードに統合される
  - **絞り込み**：一覧の絞り込み行に「行きたいリスト」ドロップダウンを追加（`wishlistListName`の値ごとに件数付き、「リスト由来ではない」も選択可。都道府県・マイカテゴリー等と同様に`setupDropdownFilters`で動的生成）
  - **「行きたい→行った」バッジ**：`wishlistListName`があり、かつ評価/コメントもある行（＝行ってみたいリストにあった場所に実際に行った）に、一覧のスポット名セルへ小さいバッジ（`badge-check`アイコン、ツールバーの✨バッジと同系統の意匠）を表示し、ホバーで元のリスト名を確認できる
  - **JSONのみロスレス**：4フィールドは`buildBackupJSONPayload`/`parseAppBackupJSON`（JSONバックアップ・Drive・ローカルキャッシュ）には含めるが、CSVフルエクスポート（`exportCSV`）には含めない。既存の`googleCategoryRaw`と同じ「JSONのみロスレス、CSVは非対応」という前例にならう
  - 回帰テストを`tests/savedListCSV.test.js`（`isSavedListCSV`/`parseSavedListCSV`）、`tests/deduplicatePlaces.test.js`（行ってみたい由来レコードとクチコミ由来レコードの双方向マージ）に追加
  - **「ログを手動で追加」「スポット情報をGeminiで調べる」からも「行ってみたい」として登録できるように（2026-07-28実装）**：Takeout由来のCSV取り込みだけでなく、ユーザー自身がまだ行っていない場所を見つけた／思いついた時にも同じ軸で管理したいという要望を受けて追加。手動追加フォームに「「行ってみたい」として追加する（まだ行っていない場所）」チェックボックスを、スポット検索候補カードにも同様のチェックボックスを追加し、どちらもチェック時は`wishlistListName: "行ってみたい"`を設定する（CSV取り込みと同じ文字列を使うため、絞り込みドロップダウンで1つのバケットに集約される）。手動追加フォームは編集時に既存の`wishlistListName`の有無をチェック状態へ反映し、外せば通常のログに戻せる（`wishlistMemo`等の他3フィールドはこのフォームでは触れず、CSV由来の値があれば保持される）。回帰テストを`tests/buildManualPlaceFields.test.js`・`tests/placeLookup.test.js`に追加

### 2.2 データ加工・分類
- **都道府県の自動判定**（`extractPrefecture`）: **住所**→都道府県名の直接一致 → 英語県名 → 主要都市名からの推測、の順に判定し、住所から判定できなければ**座標**（県庁所在地への距離が最も近い県）で判定。**店名からの推測は最後の手段**としてのみ使う（住所・座標のどちらもない場合に限る）。店名に含まれる地名は「北海道ラーメン」のようなご当地スタイルを表すブランド名であることが多く、実際の所在地と無関係なことがあるため（回帰: 福岡県の店が店名の「北海道」だけで北海道と誤判定されていたバグを2026-07-21に修正。詳細は4節）
- **カテゴリー自動分類（ヒューリスティック・フォールバック）**（`classifyCategory`）: 12カテゴリー（グルメ6種＋観光/寺社仏閣/宿泊/ショッピング/交通機関/その他）に、店名・コメントの正規表現キーワードマッチで分類。誤判定防止のための除外パターン（例:「イン」で終わる語からホテルを判定するが「ワイン」「デザイン」等は除外）も作り込まれている。**Gemini等でまだ実データ取得していないスポットの`category`初期値**として使われる（下記）
- **Google連動カテゴリーの実データ取得（Geminiプロンプト方式）**（2026-07-21実装、2026-07-21に「12種へ丸め込まずそのまま使う」方式へ設計変更。ボタン名は2026-07-26に「Google連動カテゴリーをGeminiで調べる」へ改名）: ヘッダーの「Google連動カテゴリーをGeminiで調べる」ボタン→モーダルで、①スポット名・住所のみ（評価・レビュー本文は含めない）を含むプロンプトを生成→②ユーザーがGeminiにコピー＆貼り付けして実行→③返ってきたJSON配列（`[{"id":..,"category":".."}]`）をモーダルに貼り戻して「適用する」、の3ステップ。差分方式で、`googleCategoryRaw`（Geminiが返した生の業種ラベル）が未設定のスポットのみを対象にし、1バッチ最大50件（`GEMINI_CATEGORY_BATCH_SIZE`）ずつ処理、適用後は自動的に残りの未取得分の次バッチプロンプトに切り替わる。
  - **「そのまま使う」方式**：Geminiが返した業種ラベルは、固定12種へのマッピングを一切せず**そのままGoogle連動カテゴリーとして採用**する（`getOrCreateGeminiCategory`）。同じラベルは常に同じキー（`gemini_<ラベル>`）・同じ色（ラベル文字列からのハッシュで決定的に生成、`colorForGeminiLabel`）に集約される。ラベルの種類数は青天井（`geminiCategories`レジストリに増えていく）だが、同一ラベルは自動的に1つに集約されるため、実運用では店名の分だけ増えるわけではない
  - 適用結果はモーダル内に「スポット名: 「Geminiの回答」→ 分類されたカテゴリー名」の一覧としてその場で表示され、何がどう反映されたか確認できる。一覧テーブルのGoogle連動カテゴリー列にも✨バッジが付き、ホバーで生ラベルを確認できる
  - JSONバックアップのエクスポート/インポートは`googleCategoryRaw`を保持し、再インポート時は常にこの生ラベルから`category`を再生成する（保存されていた`categoryKey`より優先。自己修復的）。CSVは他のマイカテゴリー同様に生ラベルの列を持たないため保持しない（4節）
  - Google Places API方式（実データ取得のもう一つの選択肢）は未着手
- **Googleマップリンク・緯度経度のGemini取得**（2026-07-25実装。ボタン名は2026-07-26に「リンク・緯度経度をGeminiで調べる」へ改名）: 上記のカテゴリー取得と全く同じ「プロンプト生成→貼り付け→適用」の差分バッチ方式（`GEMINI_LOCATION_BATCH_SIZE`＝50件）で、ヘッダーの「リンク・緯度経度をGeminiで調べる」ボタンから利用できる。対象は`url`または`lat`/`lng`のいずれかが未設定のスポット（`getGeminiLocationIncompletePlaces`）。評価・レビュー本文はプロンプトに含めない（既存のカテゴリー取得・スポット検索と同じプライバシー配慮）
  - **空欄埋めのみ**：Googleマップリンク・緯度経度のどちらも、既に値がある項目は上書きしない。Geminiが「確信が持てない項目はキー自体を省略する」よう指示しており、`parseGeminiLocationResponse`側もurl/lat/lngをすべて任意項目として扱う
  - **緯度経度のハルシネーション対策（2026-07-28、隔離＋要確認フラグ方式に変更）**：スポット検索機能（`checkPlaceLookupCoordinateMismatch`）と同じ「住所由来の都道府県」と「座標から最寄りと判定した都道府県」の食い違いチェックを流用。以前は食い違う場合に緯度経度を反映せず警告のみ表示していたが、これだと該当スポットは`lat`/`lng`が未設定のまま残り、`getGeminiLocationIncompletePlaces`の対象から外れないため**次回以降のバッチにも同じスポットが毎回出てきてしまい、同じ場所を何度も調べ直す羽目になる**という問題があった（ユーザー報告）。座標はいったん登録した上で`place.locationNeedsReview = true`／`locationReviewReason`を立てて隔離する方式に変更（3節データモデル参照）。これにより座標が確定するので次回以降のバッチには出てこなくなる。ヘッダーの「緯度経度 要確認」ボタン（未確認件数をバッジ表示）から専用モーダル（`renderLocationReviewList`、削除済みスポットと同じカード形式）を開き、理由を確認した上で「確認OK」を押すと`locationNeedsReview`だけが外れる（座標・スポット自体は変更しない）。ロスレスなJSONバックアップにのみこの2フィールドを保持し、CSVは他のGemini由来フィールド同様に非対応（4節）
  - 適用結果はモーダル内に「スポット名: リンクを反映 / 緯度経度を反映 / 緯度経度を反映（⚠️ 要確認: 理由）」の一覧として表示
  - **既知のURL（knownUrl）を手がかりに渡し、住所・都道府県も導出（2026-07-28実装）**：「行ってみたい」リストCSV由来のスポットは、住所・緯度経度は未設定だが本物のGoogleマップリンクは既に分かっている。`buildGeminiLocationPrompt`は`p.url`が既にある項目に`knownUrl`として渡し、名前だけからの推測より正確にその場所を特定できるようにする。あわせてGeminiには住所（`address`）も可能なら返してもらい、`applyGeminiLocationResults`で空欄埋めのみ反映。住所または座標が新しく分かった際、都道府県が未確定（既定値の「その他・海外」）のままなら`extractPrefecture`で再判定する（既に確定済みの都道府県は上書きしない）。これにより一覧の「地図の中心に表示」ボタン（`lat`/`lng`が無いと表示されない）も自動的に使えるようになる。ハルシネーションチェック（`checkPlaceLookupCoordinateMismatch`）も、既存の住所が空でも今回新しく分かった住所を使うように修正（従来は住所が空だと必ず素通りしていた）
  - **Geminiが見つけられなかったスポットが毎回のバッチに出続けていた不具合を修正（2026-07-28実装）**：座標の食い違いは要確認フラグで隔離済みだったが、Geminiがそもそも何の情報も返せなかった（url/住所/座標のいずれも反映できず「反映できる項目がありませんでした」表示になる）スポットには何のフラグも立たず、`getGeminiLocationIncompletePlaces`の対象に残り続けるため、次回以降のバッチにも毎回出てきてしまっていた（ユーザー報告）。`applyGeminiLocationResults`は、url/住所/座標のいずれも反映できなかった項目に`locationLookupSkipped = true`を立てて以後の自動バッチ対象から除外するようにした。ヘッダーの「リンク・緯度経度 要確認」モーダルに「Geminiが見つけられなかったスポット」セクションを追加し、「もう一度調べる対象に含める」（`retryLocationLookup`）でフラグを外して再度バッチ対象に戻せる。バッジの件数は座標要確認・Gemini未検出の両方を合算。`locationLookupSkipped`はJSONバックアップにのみ保持（他のGemini関連フラグと同じ方針）
- **Geminiモーダル（カテゴリー／リンク緯度経度／スポット検索）のレイアウト調整（2026-07-28実装）**：①プロンプト表示欄・②貼り付け欄が広すぎて③適用結果欄が狭く見づらいという指摘を受け、①②の`rows`を減らし`.gemini-cat-textarea-compact`クラス（min-height 50px）を3モーダル共通で適用、③の`.gemini-cat-result-list`側は基本の高さを220px→320pxに、既存の拡大版`.gemini-cat-result-list-large`（リンク緯度経度モーダルで先行採用済みだったもの）を420px→560pxにして、カテゴリーモーダルの結果欄にも同じ拡大クラスを適用した
- **マイ都道府県／マイカテゴリー（2軸分離）**: Google連動側（自動判定結果）とは別軸で、各行を一覧の「マイカテゴリー」列のドロップダウンから手動上書き可能。上書きした値（`myPrefecture`/`myCategory`）は再インポート時も保護される。マイカテゴリーの選択肢は**ユーザー自作のマイカテゴリー＋既に登場したGoogle取得カテゴリー（Gemini）のみ**（標準12カテゴリーは選択肢から外している。理由は下記「マイカテゴリーの管理」参照） —— 例えば「ラーメン店」「うどん屋」「ハンバーガー店」という別々のGoogle連動カテゴリーを、マイカテゴリー側で共通の自作カテゴリー「グルメ」にまとめる、といった使い方ができる
- **マイカテゴリーの管理**: ユーザーが独自カテゴリー（名前・色）を作成・削除できる管理モーダル（`registerCustomCategory`/`deleteCustomCategory`）。作成したカテゴリーは各行のマイカテゴリー欄で選択可能
  - **方針転換（2026-07-21）**：当初はマイカテゴリーの選択肢に標準12カテゴリーも含めていたが、Google連動カテゴリー側がGemini取得の実データ（生ラベルそのまま）を持てるようになったことで、標準12カテゴリーの出番自体がなくなったため、マイカテゴリーは「完全にユーザー定義」の軸にする方針に変更。既存データ（過去のJSON/CSVインポート等）で`myCategory`が標準12種のいずれかに設定済みの行は、その行の選択肢にだけ例外的に残すことで「勝手にリセットされたように見える」ことを防いでいる（新規に選び直すことはできない）

### 2.3 表示・操作
- **統計カード**: 総スポット数／制覇した都道府県数／**最多カテゴリー（Google連動）**／**最多カテゴリー（マイカテゴリー、未設定行を除いて集計）**の4枚（2026-07-21実装：それまでは実効値ベースの単一カードだった）
- **一覧テーブル**: **列順は「スポット名→最終更新日→評価→操作→都道府県→Google連動カテゴリー（読み取り専用、生ラベルそのまま表示）→マイカテゴリー（編集可能なドロップダウン）→住所（横スクロール対応）→レビュー本文」（2026-07-24並び替え）**。操作は地図中心表示／削除、手動入力エントリのみ編集も可。カテゴリーが2列に分かれているため、Google側の細かい分類とマイカテゴリー側の大づかみな分類を同時に見比べられる（2026-07-21実装：それまでは1列で実効値のみ表示していた）。
  - **列順の並び替え（2026-07-24実装）**：「このお店いつ行ったっけ？」「地図で見よう」のような使い方では、名前を見た瞬間に日付・評価・操作までスクロールなしで見えてほしい、というユーザーからのフィードバックを受けて変更。従来は名前の次が都道府県〜住所〜レビュー本文と続き、日付・操作列に辿り着くには横スクロールが必須だった（実データ量ではスクロールしても操作列が画面外という表示崩れも別途発生していた。4節参照）。都道府県・カテゴリー・住所・レビュー本文は上部の絞り込みドロップダウンで既に選別した上で見る情報という位置づけのため、スクロール側（後方）へ回した。この並びはモバイルのカード表示（768px以下）にもそのまま反映される（DOM上の列順がカード内の項目の縦積み順と一致する仕様のため）
  - **マイカテゴリー列の表示（2026-07-21実装）**：未設定の行はGoogle連動カテゴリー列と同じ名前・同じ見た目でそのまま表示する（「未設定（〇〇のまま）」のような説明的な文言は出さない）。実際に上書きしている行だけ✨バッジを表示して区別する（Google連動カテゴリー列でGeminiの生ラベルを示すのと同じバッジを、こちらは「手動上書きあり」の意味で流用）
  - 列ヘッダークリックでソート（現状はGoogle連動カテゴリー列のみソート対応。マイカテゴリー列のソートは未対応）。**デフォルトは最終更新日の新しい順（`updateTime`降順、2026-07-26変更）**：従来はスポット名の昇順がデフォルトで、「自分が今しがた編集・取り込んだ内容がちゃんと反映されているか」をひと目で確認しづらいというユーザーからのフィードバックを受けて変更
    - **「最終更新日」列の紐付け先バグ修正（2026-07-26）**：列の表示・ソート・上部の日付範囲フィルターがいずれも`publishTime`（初回投稿日）を参照しており、`updateTime`（Google側の最終更新日／手動編集時の投稿日。2.1節のマージロジックが前提としている「最終更新日」はこちら）ではなかった。ラベルと実際の挙動がズレていたため`updateTime`に統一。**注意**：`updateTime`はマイカテゴリードロップダウンをその場で変更しただけでは更新されない（Takeout再取り込みでのGoogle側更新検知、または手動追加・編集フォームでの投稿日入力時のみ更新される）ため、軽微な上書き操作まで追跡したい場合は別の仕組みが必要
    - **ソート矢印アイコンが一度も切り替わっていなかった不具合を修正（2026-07-26）**：列ヘッダークリック時のアイコン更新処理が`th.querySelector("i")`でアイコン要素を探していたが、`lucide.createIcons()`は初回描画時に`<i data-lucide="...">`を`<svg data-lucide="...">`へ置き換える（タグ名がiではなくなる）ため、2回目以降は何も見つからず矢印の向きが変わらないまま放置されていた。タグ名を問わず`[data-lucide]`属性で辿るように修正し、初期表示時にもデフォルトソートに合わせて矢印を反映するようにした（`updateSortIcons`関数に切り出し）
- **検索・フィルター**: 名前/住所/コメントの部分一致検索、都道府県、**Google連動カテゴリーとマイカテゴリーは独立した2つのドロップダウンで別々に絞り込み可能（両方指定した場合はAND条件。マイカテゴリー側は「未設定」でも絞り込める）**（2026-07-21実装：それまでは実効値の単一ドロップダウンだった）、評価・最終更新日の範囲での絞り込み（件数付きドロップダウン）
- **インタラクティブマップ**（Leaflet、ダークタイル）: フィルター結果を円マーカーで表示、クリックでポップアップ（名前・県・カテゴリー・評価・コメント・Googleマップリンク）
- **グラフ**（Chart.js）: カテゴリー比率（ドーナツ、**Google連動／マイカテゴリーの集計軸をトグルで切り替え可能**、2026-07-21実装）、都道府県別件数トップ10（横棒）
  - Google連動カテゴリーはラベルの種類数が青天井になりうるため、件数の多い上位8件のみを個別スライスにし、残りは「その他（上位8件以外）」に集約して凡例が際限なく伸びないようにしている（`MAX_CHART_CATEGORY_SLICES`）。この集約はチャート表示のみの措置で、一覧・フィルター・エクスポートでは常に元のカテゴリーがそのまま扱われる
  - 都道府県×カテゴリーのクロス集計（ピボットテーブル）はアプリ内には実装しておらず、CSVエクスポート後にExcel等のピボットテーブル機能を使う運用を想定（ユーザー確認済み、2026-07-21）
- **エクスポート**: CSV（BOM付きUTF-8、Excel対応）／JSON、いずれもダウンロード
  - **使い分けの方針**：**JSON＝完全なバックアップ・復元用**（`categoryKey`/`myCategoryKey`/`myCategoryName`/`myCategoryColor`等キー情報ごと保持するため、手動入力の`source`やマイカテゴリー・マイ都道府県、未登録の独自マイカテゴリーまで含めて一切失わずに再読み込みできる）。**CSV＝Excelで見る・一括編集するための出力**（カテゴリー等は表示名で書き出すため人間が読みやすい一方、そのカテゴリーがまだ一度も作られていない状態での復元はできない、という構造的な制約がある。詳細は4節）。データを完全に保全したい場面（バックアップ、機種変更、リセット前退避）は必ずJSONを使うこと
- **リセット**: 確認ダイアログ後、全データ・フィルター・マップをクリア

### 2.4 UI/UX
- ダークテーマ、ガラスモーフィズム調のカード、グラデーションアクセント
- レスポンシブレイアウト対応（768px以下は一覧テーブルをカード表示に切り替え）

### 2.5 Googleドライブ連携（2026-07-24実装・本番URLで連携／保存／別セッションからの読み込みまで実機確認済み）
- ヘッダー常時表示（アップロード前でも操作可能）の「Googleドライブと連携」ボタンから、Google Identity Services（GIS）のimplicitトークンクライアントでOAuth認可。スコープは`drive.file`（このアプリが作成したファイルのみアクセス可）に限定
- 連携が成功すると自動的にDrive上の保存データ（固定ファイル名`g-map-dashboard-backup.json`）を検索・読み込み。既存の`parseAppBackupJSON`＋`deduplicatePlaces`を経由してローカルの`places`にマージするため、ファイルインポートと同じ「既存の手動編集を上書きしない」安全性が保たれる
- 「Driveに保存」ボタン（連携後のみ表示）で、JSONエクスポートと全く同じデータ構造（`buildBackupJSONPayload`。エクスポート用と共通化済み）をDriveへアップロード。初回は新規作成（POST）、2回目以降は同じファイルを更新（PATCH、`driveFileId`で追跡）
- アクセストークンはブラウザメモリ上のみ（`driveAccessToken`）。ページをリロードすると失われ、再度「連携」ボタンを押す必要がある
  - **トークン永続化は見送り（フェーズ3実装時に検討し、対象外と判断・2026-07-25）**：フェーズ3（ローカルキャッシュ）と合わせて検討予定としていたが、実装時に見送りを決定。使用しているGoogle Identity Services（GIS）のimplicitトークンフローにはrefresh tokenが無く、有効期限（約1時間）内の再クリックを省略できる程度のメリットしかない一方、生きたOAuthアクセストークンをブラウザストレージに残すことは閲覧データ自体を残すより実質的にリスクが高いと判断したため。ローカルキャッシュ自体（`places`データのIndexedDB保存）は実装済み（4節参照）だが、これはDriveのアクセストークンとは別物
- 複数端末の同時編集は想定せず、「開いたら毎回Driveの最新を取得して上書き」という単純な方針（5節参照）

## 3. データモデル（1件あたり）

```js
{
  id, name, address, lat, lng,
  prefecture, category,          // Google連動軸。googleCategoryRawがあればgetOrCreateGeminiCategoryで
                                  // 生成した `gemini_<ラベル>` キー、なければclassifyCategoryの12種キー
  googleCategoryRaw,             // Gemini/Places API等から取得した生の業種ラベル（未取得はnull）。
                                  // 12種への丸め込みはせず、そのままcategoryの名前として使う
  myPrefecture, myCategory,      // マイ都道府県／マイカテゴリー軸（手動上書き。未設定はnull）。
                                  // myCategoryは標準12種・自作マイカテゴリー・Google取得カテゴリーのいずれも参照しうる
  rating (1-5 or null),
  comment, url, source,          // "保存済みの場所" / "クチコミ投稿" / "CSVインポート" / "サンプルデータ" / "手動入力" / "行きたいリスト"
  publishTime, updateTime,       // "YYYY/MM/DD" 文字列。publishTimeという名前だが、
                                  // 一覧・CSV・ソート・フィルターでは実質「最終更新日」として
                                  // 扱う（フィールド名は歴史的経緯でそのまま。詳細は4節）
  locationNeedsReview,           // 2026-07-28追加。「リンク・緯度経度をGeminiで調べる」で座標を
                                  // 住所と食い違ったまま登録した際に立つ要確認フラグ（既定false）。
                                  // ヘッダーの「緯度経度 要確認」モーダルで内容確認後「確認OK」を
                                  // 押すと外れる。JSONバックアップのみ保持、CSVは非対応
  locationReviewReason,           // 上記フラグが立った理由（都道府県の食い違い内容の文字列、未設定はnull）
  wishlistListName, wishlistMemo, // 2026-07-28追加。Googleマップのカスタムリスト
  wishlistTags, wishlistComment  // （「行ってみたい」等）取り込み由来のフィールド（すべて任意）。
                                  // 既存のcomment（クチコミ本文）とは別物として区別する。
                                  // JSONバックアップのみ保持、CSVは非対応（2.1節参照）
}
```

カテゴリー名・色のルックアップは常に `getAllCategories()`（`CATEGORIES`固定12種 + `customCategories`自作 + `geminiCategories`のマージ）経由で行う。
表示・フィルター・ソートで「実効値」（マイカテゴリー優先）が必要な場面（フィルター、地図、統計カード等）は `getEffectivePrefecture`/`getEffectiveCategory`（`myPrefecture || prefecture` 等）を使うが、一覧テーブルのGoogle連動カテゴリー列のように**あえてGoogle連動側だけを常時表示したい場面**では `p.category` を直接参照する（2.3節）。

## 4. 既知の制約・設計上の注意点

- **`publishTime`／`updateTime`の2軸日付は設計ミスだった（2026-07-26判明・修正）**：当初は「`publishTime`＝初回投稿日、`updateTime`＝最終更新日」という2軸のつもりで設計したが、実際にはGoogle Takeoutは真の初回投稿日を出力せず、`publishTime`というキー名の日付フィールドが**レビューを編集するたびに更新されて返ってくる**（つまり実質「最終更新日」）。一方`updateTime`に対応する実データはほぼ存在せず、`extractUpdateTimeRobustly`はTakeoutの実ファイルに対してはほぼ常に空を返す。ユーザー自身がこの前提を早期に把握できていなかったため、2軸の日付という誤った設計のまま実装が進んでいた
  - **影響していた箇所**：一覧の「最終更新日」列・デフォルトソート・日付範囲フィルターは、2026-07-26の一連の修正の中で一時的に`updateTime`を参照するよう変更したが、これは上記の実態を踏まえると誤りだったため`publishTime`に戻した（列のラベル「最終更新日」はそのまま、参照先フィールドだけが内部的に`publishTime`という名前）
  - **CSVエクスポートの列を統合（2026-07-26）**：フルCSVエクスポート（`exportCSV`）の「初投稿日」「最終更新日」の2列を「最終更新日」1列に統合（中身は`publishTime`）。`parseAppCSVBackup`は後方互換のため、旧フォーマット（両方の列があるCSV）を読み込んだ場合は「初投稿日」列を優先して`publishTime`に復元する。回帰テストを`tests/parseAppCSVBackup.test.js`に追加
  - **JSONバックアップ（`buildBackupJSONPayload`/`parseAppBackupJSON`）は変更していない**：`updateTime`フィールド自体は引き続き保持・復元する（後述の追従上書きロジックが参照しているため、ロスレスバックアップとしての互換性を優先）
  - **「Google側の追従上書き」の新旧判定もpublishTimeベースに修正（2026-07-26）**：2.1節の「Google側でレビュー・評価が編集された場合の追従上書き」機能（2026-07-22実装）は`item.updateTime`が新しいかどうかで判定していたが、実データでは`updateTime`がほぼ常に空のため、**実運用では追従上書きの条件がほぼ成立せず、常に「空欄埋めのみ」にフォールバックしていた**（=2026-07-22時点の実装は、テスト上は正しくてもTakeoutの実データに対しては機能していなかった可能性が高い）。`deduplicatePlaces`の新旧判定を`item.publishTime`ベースに変更し、`tests/deduplicatePlaces.test.js`の該当テストも合わせて書き換えた
- **マイ都道府県はCSVエクスポートの列から外した（2026-07-26）**：ユーザーの運用上ほぼ使われていない（マイカテゴリーほど活用されていない）ため、フルCSVエクスポート（`exportCSV`）の列からは削除。ただし**データモデル・一覧の都道府県ドロップダウン（`myPrefecture`上書きUI）・JSONバックアップは変更していない**——「今は使っていないが、将来別の用途で項目名を変え[換え]て再利用できるように」というユーザーの意図により、値の入れ物自体は残す方針。`parseAppCSVBackup`側は元々`csvField`が存在しない列に対して安全に`""`を返す作りだったため、読み込み側の対応は不要（列が無いCSVは`myPrefecture: null`として復元される）
- **状態は基本メモリ上のみ、ただしローカルキャッシュ（IndexedDB）で継続可能に（フェーズ3、2026-07-25実装）**：以前は「リロードで全データが消える（永続化なし、localStorage等未使用）」だったが、ヘッダーの「このデバイスにデータを保存」トグル（**初期値ON**）がONの間は、`markUnsavedChanges()`発火のたびに約800msデバウンスで`places`一式（`buildBackupJSONPayload()`と同じ形＝JSON/Drive出力と共通）をIndexedDB（`g-map-dashboard-cache`、単一レコード）へ書き込む（`scheduleLocalCacheWrite`/`writeLocalCache`）。次回起動時、トグルONならDOMContentLoadedで自動的に読み込み・復元し（`restoreFromLocalCache`）、アップロード画面を経由せずダッシュボードが開く。**トグル自体の設定値だけは例外的にlocalStorageに保存**（`localCacheEnabled`。実データではなく設定値1個であり、起動時に同期的に判定する必要があるため）。IndexedDB自体が使えない環境（プライベートブラウジング等）や壊れたキャッシュは全経路で無音フォールバックし、今まで通りの空のアップロード画面になる（例外を投げてアプリを壊さない）
    - **共有PC向けのOFF＋即時削除**：トグルをOFFにすると、以後の書き込みが止まるだけでなく、既存のキャッシュも即座に削除する（`clearLocalCache`）。書き込みを止めるだけでは共有PCに平文データが残り続けてしまうため
    - **リセットとの整合**：`resetApp()`はトグルの状態に関わらずIndexedDBのレコードも必ず削除する。そうしないと「リセットしたのに次回リロードで復活する」という事故になるため
    - **Drive連携との関係**：ローカルキャッシュは「端末単位のオフライン継続」が目的で、Drive連携（複数端末をまたぐ同期）とは役割が異なる。`loadFromDrive()`成功時もキャッシュを更新するが、`hasUnsavedChanges`フラグを下ろす対象（下記）にはローカルキャッシュを含めない——端末ローカルの保存であってDrive/JSONのような「他の端末からも見える」保存ではないため
  - **うっかり閉じる事故の防止（2026-07-24実装）**：自動保存が無い以上、保存を忘れてタブを閉じる／リロードすると編集が跡形もなく消えるため、`hasUnsavedChanges`フラグと`beforeunload`ハンドラで警告するようにした。ファイル取り込み・手動追加/編集・削除・マイ都道府県/マイカテゴリーの上書き・Gemini適用・マイカテゴリー作成/削除のいずれかが起きるとフラグが立ち、**ロスレスな保存**（Driveへの保存 `saveToDrive`、またはJSONエクスポート `exportJSON`）が成功するとフラグが下りる。CSVエクスポートは既知の通り非可逆（4節前段参照）なので、あえてフラグを下ろす対象に含めていない（「CSVを出したから安心」という誤った安心感を防ぐため）。ローカルキャッシュへの保存もこのフラグを下ろさない（上記）。「サンプルデータをロード」は実データではないため対象外（意図的）
    - **常時表示の未保存インジケーター（2026-07-26実装）**：`beforeunload`はタブを閉じる／リロードする瞬間にしか発火しないため、タブを開きっぱなしで作業を続けている間は「保存し忘れているかどうか」に気づく手段が無かった（ユーザーからの指摘）。ヘッダーの「Driveに保存」ボタンの隣に`#unsaved-indicator`バッジを追加し、`hasUnsavedChanges`がtrueの間は常時「未保存の変更があります」を表示するようにした（`markUnsavedChanges`/`clearUnsavedChanges`から`updateUnsavedIndicator()`を呼ぶだけの単純な仕組みで、フラグを下ろす条件は上記のbeforeunload警告と完全に共通）
- **外部CDN依存**：オフライン環境や配布用アプリ化の際は、Leaflet/Chart.js/Lucide/フォントをローカル同梱する必要あり
- **1920×1080でのレイアウト崩れ・スクロール不能バグ（2026-07-24修正）**：実データ（600件弱、長い店名・住所を含む）を読み込むと、「操作」列を含む一覧テーブルの右側が画面外にはみ出し、しかも一切スクロールして辿り着けない不具合があった。原因は`#dashboard-section`（`.dashboard-grid`）がCSSクラス上は`flex-direction: column`だが、`showDashboard()`が実行時に`style.display = "grid"`をインライン指定するため実際には**単一列の暗黙グリッド**として動作しており、そのグリッドアイテム（`.table-card`等）のデフォルト`min-width: auto`（＝中身の最大幅）により、一覧のフィルター行（検索欄＋5つのドロップダウン＋日付範囲）が折り返されずに際限なく広がり、`body`の`overflow-x: hidden`で見えないまま切り捨てられていたため。`.dashboard-grid > *`に`min-width: 0`を設定して解消（`.filter-row`側の`min-width: 0`／`.card-title`の`flex-shrink: 0; white-space: nowrap;`も合わせて調整）。同時に見つかった関連の不具合（ヘッダーのタイトル圧縮、`thead`の半透明による行の透け）も同日中に修正済み（本節前段・2.3節参照）
- **カテゴリー分類は正規表現ベースのヒューリスティック**：厳密なNLPではないため、新しい店名パターンに弱い可能性がある（手動編集で救済可能な設計にはなっている）
- **重複排除ロジック**は完全ではない（URL/座標/名前ベースの近似マッチ）。重複と判定された場合のマージ挙動（`updateTime`が新しい取り込みはGoogle由来フィールドを追従上書き）は2.1節を参照
  - **キー生成とマージ相手検索の不一致バグ（2026-07-25修正）**：`deduplicatePlaces`は「同一スポットか」を判定するキーをURL→緯度経度→名前+住所の3段階で作るが、実際にマージ相手（`existing`）を探す処理はURL・緯度経度の2段階しかチェックしておらず、名前+住所だけで一致するケースが漏れていた。該当するレコード（URL・緯度経度のどちらも無い手動追加スポット等）は「重複とは認識される（＝別行としては追加されない）」のに「マージ相手が見つからず取り込んだ内容が一切反映されない」というサイレントなデータ消失を起こしていた（ユーザー報告: Gemini検索で追加した「circolo park 鴻巣店」でCSV再取り込みの内容が反映されなかった）。キー生成とマージ相手検索を同じMapで一本化し、この矛盾自体を構造的に解消。回帰テストを`tests/deduplicatePlaces.test.js`に追加
  - **手動入力レコードの空欄埋めに初投稿日・最終更新日が含まれていなかった不具合（2026-07-25修正）**：`deduplicatePlaces`の「空欄埋めのみ」マージ経路は評価・レビュー・住所は既存が空欄なら埋めるのに、初投稿日・最終更新日だけ埋める処理が漏れていた。手動入力レコードは常にこの経路を通る仕様のため（下記）、Gemini検索で追加した評価・日付未設定のスポットをCSVで埋めて再取り込みしても、最終更新日だけは絶対に反映されなかった（評価・レビューは既に空欄埋め対象だったため問題なし）。対象フィールドに追加して解消
- **Google連動フィールドの追従上書きは`updateTime`頼み**：Takeout側が`updateTime`を正しく返さない/欠落しているケースでは新旧判定ができず、従来通り「空欄埋めのみ」にフォールバックする（＝Google側の編集を見逃す可能性が残る）。日付を見ない「常に新データで上書き」方式は採用していない（手動入力レコードとの区別を`source`だけに頼る設計との相性を優先したため）
- **日付のゼロ埋め表記ゆれとその比較バグ（2026-07-25修正）**：`parseAppCSVBackup`（このアプリ自身の全件CSVエクスポートの再取り込み）だけが初投稿日・最終更新日をゼロ埋め整形（`formatDateString`）せず生の文字列のまま格納していたため、Excel等で編集すると`"2021/1/5"`のようにゼロ埋めが崩れた表記と、他経路由来の`"2021/01/05"`が混在するようになっていた。さらに一覧のソート・日付範囲フィルター（`matchesDateRange`）・Google追従上書きの新旧判定（`normalizeDateForCompare`）がいずれも単純な文字列比較だったため、ゼロ埋めが揃っていないと辞書順とカレンダー順がズレて明らかにおかしい並び順・絞り込み結果になっていた（例:`"2021/9/1"`と`"2021/10/1"`は文字列比較だとOct<Sep扱い）。`parseAppCSVBackup`を他の取り込み経路と同じく`formatDateString`で整形するように修正し、加えて`normalizeDateForCompare`自体を正しくゼロ埋めするように直した上でソート・フィルターの両方をこの関数経由にし、既存データに残る表記ゆれがあっても正しく動くようにした。回帰テストを`tests/matchesDateRange.test.js`・`tests/parseAppCSVBackup.test.js`に追加
- **CSVは完全な無劣化バックアップ形式ではない**：メイン画面のCSVエクスポート（`exportCSV`）はカテゴリー・マイカテゴリーを内部キーではなく表示名で書き出す（Excelでの可読性を優先した設計）。再インポート時（`parseAppCSVBackup`）は表示名→キーの逆引きで復元するため、**既に登録済み／標準12種のカテゴリーは問題なく復元できる**が、**まだ一度も作成していない独自マイカテゴリー名・Google取得カテゴリー（Gemini）名だけは色情報が無く復元できず、その行は上書きなしにフォールバックする**。同様に、Geminiで取得した`googleCategoryRaw`（生の業種ラベル）もCSVには列として出力していないため、CSVだけを介した再読み込みでは失われる（既存の在メモリレコードとの再統合時は埋め合わせで保護されるが、ブラウザ再読み込み後にCSVのみから復元する場合は再取得が必要）。完全に無劣化でバックアップ・復元したい場合は必ずJSONエクスポートを使うこと（`parseAppBackupJSON`は`categoryKey`・`googleCategoryRaw`等キー情報ごと保持し、再インポート時は`googleCategoryRaw`から`category`を再生成するためこの制約がない）
  - **未登録マイカテゴリー名の警告（2026-07-24実装）**：上記の「上書きなしにフォールバック」が従来は無言で起きていた（ユーザーが「登録したつもりが反映されていない、なぜ？」と気づけない）ため、`parseAppCSVBackup`が逆引きに失敗した行のマイカテゴリー名を`unresolvedMyCategoryNames`として集計して返すようにし、`handleFiles`のCSV取り込み完了後に「マイカテゴリー「◯◯」(n件)は未登録のため反映されませんでした。先に『マイカテゴリー設定』で作成してから、もう一度CSVを読み込んでください」という1回のアラートで通知するようにした。自動登録（黒魔術的に色を決めて新規カテゴリー化する）ではなく、あくまで通知に留める方針（色を決めずに勝手にカテゴリーが増えると後で混乱するため）。回帰テストを`tests/parseAppCSVBackup.test.js`に追加
- **Google連動カテゴリー（Gemini取得分）の色はブランドカラーではなく、ラベル文字列から決定的に生成した色**（`colorForGeminiLabel`、HSLハッシュ）：地図ピン等の個別表示用としては十分だが、カテゴリー比率チャートの配色そのものについてはCVD（色覚特性）配慮のバリデーションは行っていない（開放集合のラベルに対して固定の検証済みパレットを割り当てるのが難しいため）。チャートの凡例が際限なく伸びないよう上位8件＋「その他」に集約する対策のみ実施（2.3節）
- テストは `tests/`（Node標準 `node --test`、`npm test`で実行）に `classifyCategory`／`extractPrefecture`／`parseAppBackupJSON`／`buildManualPlaceFields`／`matchesDateRange`／`parseCoordinatePair`／`parseCSVRows`／`parseAppCSVBackup`／`getOrCreateGeminiCategory`・`buildGeminiCategoryPrompt`・`parseGeminiCategoryResponse`・`applyGeminiCategoryResults`（`tests/geminiCategory.test.js`）／`getGeminiLocationIncompletePlaces`・`buildGeminiLocationPrompt`・`parseGeminiLocationResponse`・`applyGeminiLocationResults`（`tests/geminiLocation.test.js`）／`deduplicatePlaces`の分が存在（2026-07-20〜25）。それ以外のロジックはまだ未カバー
- READMEはまだ存在しない

## 5. 今後の要望・ロードマップ

*（ここにやりたいことを追記していく欄）*

### アプリ化・アクセス性
- [x] Windows / スマホで「ブラウザを経由せず起動できる」形にしたい（2026-07-26実装。フェーズ4、詳細は6節）→ マニフェスト＋Service Workerでホーム画面への追加・スタンドアロン起動に対応。フルオフライン対応（CDN依存の解消）は範囲外のまま残課題
- [x] Googleアカウント連動などで、スマホでもPCでもアクセスできるようにしたい（実質達成済みとして2026-07-26にチェック）→ Googleドライブ連携（フェーズ2）でどの端末からでも同じデータにアクセスできる。項目追加時点ではDrive連携が未実装だったため`[ ]`のまま残っていたチェック漏れ

**方針: 保存先はGoogleドライブにする**
- 自前サーバー・DBを持たず、Drive REST APIをブラウザから直接呼ぶ形にすることで「バックエンドなし」の設計思想を維持できる
- OAuthスコープは `drive.file`（アプリが作成したファイルのみアクセス可）に限定し、Drive全体への権限は要求しない
- Google Cloud ConsoleでのOAuthクライアント登録は必要だが、個人利用（自分のみ使用）なら「テストモード」のままでよく、審査・追加費用は不要
- [x] オフライン時にも使えるよう、ローカル（IndexedDB等）に直近データをキャッシュしつつ、開いたタイミングでDriveの最新版を取得する二層構成にする（PWA化と相性が良い）（フェーズ3・2026-07-25実装）
  - **注意点**：ローカルキャッシュは暗号化されず端末上に平文で残るため、共有PC等では望ましくない場合がある
  - **対応方針**：「このデバイスにデータを保存する」トグルを用意し、**端末ごとに**キャッシュのON/OFFを選べるようにする（例：スマホはON＝オフラインでも閲覧可、共有PCはOFF＝毎回Driveから取得しローカルに残さない）。ローカルキャッシュはそもそも端末ごとに独立しているため、端末間の連携は不要でシンプルに実現できる。実装では新規の「設定画面」は作らず、既にアップロード前から常時表示されているヘッダーの`#drive-sync`ブロックにトグルを統合した（初期値はON。詳細・技術的な設計判断は4節参照）
- 複数端末での同時編集は想定しにくいため、競合解決は「開いたら毎回Driveの最新を取得して上書き」というシンプルな方式で十分（過剰な差分マージ機構は作らない）
  - **保存時の軽量な競合検知（2026-07-24実装）**：読み込み（`loadFromDrive`）は`deduplicatePlaces`経由の安全なマージだが、保存（`saveToDrive`）はメモリ上の全データをそのままDriveへ上書きするだけで、Drive側の最新状態とはマージしない。そのため「端末Aで保存→端末Bが(Aの保存を取り込まないまま)保存」のケースではAの変更が無言で消えるリスクが残る。これに対し、**本格的なマージではなく軽量な検知＋警告**を実装：保存直前にDriveファイルの`modifiedTime`だけを取得（中身は取得しない、軽量）し、直近の読み込み/保存時に把握していた`modifiedTime`（`driveKnownModifiedTime`）と食い違っていれば「他の端末で更新されています。上書きしますか？」と確認ダイアログを出す。キャンセルすればアップロードは一切走らない。ユーザーとの合意：「同時編集はそう起きない想定なので、自動マージは過剰。ただし無言で上書きされるのは避けたいので、気づける仕組みだけ欲しい」という方針
- [ ] 個人情報の取り扱いに配慮したい（現状すでにクライアントサイド完結・サーバー送信なしの設計は維持する方針。Drive連携後も「自分のGoogleアカウントの中で完結」という原則を保つ）

### カテゴリー機能の強化

**方針: カテゴリー・都道府県ともに2軸に分離する**
- **Google連動カテゴリー**: Googleマップ側の業種情報を反映する軸。取得方法は以下2通りを選択式にする
  - a) Google Places API等で実データを取得（正確だが要課金）→ 未着手
  - b) Geminiプロンプト方式：アプリ側で「スポット名・住所のみ」を整形してプロンプト化し、ユーザーがGeminiに貼り付けて分類結果を取得、アプリに貼り戻す（追加課金なし、既存Geminiアカウントの範囲で完結）→ **実装済み（2026-07-21）**。詳細は2.2節
    - **プライバシー配慮**：評価・レビュー本文はプロンプトに含めない。名前と住所のみを渡すことで、個人的な感想・コメントの外部送信を避ける
    - **差分方式**：毎回全件ではなく「Google連動カテゴリーが未設定のスポットのみ」を対象にする。プロンプトの肥大化と、送信範囲の無駄な拡大の両方を防ぐ
    - **注意点（バッチ分割）**：差分方式でも、初回インポート時など未分類が数百件単位になる場合は1回のプロンプトに収まらない可能性があり、バッチ分割の仕組みが必要になる → 1バッチ50件で分割、適用後は自動的に次バッチへ進む形で対応
    - **注意点（出力フォーマットの固定）**：Geminiの返答が毎回同じJSON構造で返るよう、プロンプト側で厳密に指定する必要がある。ブレるとアプリ側のパースが失敗する → ```json フェンス付き回答は許容、配列以外/壊れたJSONはエラー表示にフォールバック
    - **注意点（手動ステップが挟まる）**：API方式と違い完全自動ではなく、「プロンプトをコピー→Geminiで実行→結果を貼り戻す」という操作が毎回必要になる
  - どちらの方式で取得しても、誤判定はあり得るため**手動で上書き修正可能**にする（既存のマイカテゴリー上書き機能でカバー）
  - **方針転換（2026-07-21）**：Googleの実際の業種タグは12カテゴリーよりずっと細かいため、当初は「生データを保持しつつ一覧・グラフ表示時に12カテゴリーへマッピングする」設計を想定していたが、実装・使用してみると多くのラベルが「その他」に丸め込まれてしまい実用性が低いことが判明。ユーザーからのフィードバックを受けて**12種への正規化自体をやめ、Geminiが返した生ラベルをGoogle連動カテゴリーとしてそのまま使う**方式に変更した（`getOrCreateGeminiCategory`）。細かい分類を保ったまま把握したい、というのが元々のユーザーの狙いだったため
- **マイカテゴリー / マイ都道府県**: ユーザーが自作する独自タクソノミー軸。常にユーザー所有で、インポート処理は一切触らない
  - 例：Google連動カテゴリーは「ラーメン店」のままでも、マイカテゴリーで「よく行く店」のように独自に管理できる
  - 都道府県も同じ理由（再インポートで上書きされたくない）が当てはまるため、カテゴリーと同じ2軸構成にする
- [x] 自分でカテゴリーを作成・登録できるようにしたい（2026-07-20実装）
  - マイカテゴリー軸（`myCategory`）として実装。「マイカテゴリー設定」モーダルから名前・色を指定して作成・削除できる（`customCategories`、`registerCustomCategory`/`deleteCustomCategory`）
  - Google連動カテゴリーは引き続き `CATEGORIES` 定数の12種類固定（実データ取得のa/b方式は未着手、下記）
- [x] Google連動カテゴリーの実データ取得（Geminiプロンプト方式）を実装したい（2026-07-21実装）
  - ヘッダーの「Google連動カテゴリーをGeminiで調べる」ボタンからモーダルを開き、①プロンプト生成＆コピー→②Geminiで実行→③回答JSONを貼り付けて適用、の3ステップで反映（詳細は2.2節）
  - `googleCategoryRaw`（未取得はnull）に生の業種ラベルを保持。差分方式・50件バッチで実装
  - JSON バックアップは`googleCategoryRaw`を含めて完全復元できるが、CSV往復では失われる（4節のマイカテゴリー名と同じ制約のクラス）
  - 残: Google Places API方式（選択肢a）は未着手。優先度は低い（Geminiプロンプト方式で当面の要望は満たせるため）
- [x] Geminiのカテゴリーは12種に丸め込まず、そのまま使いたい（2026-07-21実装）
  - ユーザーからのフィードバック：実際に使ってみると多くのラベルが「その他」に丸め込まれてしまい、Google連動カテゴリーの細かさを活かせていなかった
  - `mapRawCategoryToKey`（キーワードマッチで12種へ丸め込む方式）を廃止し、`getOrCreateGeminiCategory`（ラベル文字列をそのままカテゴリー名・キーとして使う方式）に置き換え。同じラベルは同じキー・同じ色に自動集約される
- [x] Google連動カテゴリー（細かい）とマイカテゴリー（大づかみ）の両方を一覧で見られるようにしたい（2026-07-21実装）
  - 「ラーメン店」「うどん屋」「ハンバーガー店」はGoogle連動カテゴリーとしては別々のままにしつつ、マイカテゴリー側で「グルメ」のようにまとめて管理したい、という要望を受けて対応
  - 一覧テーブルのカテゴリー列を「Google連動カテゴリー」（読み取り専用）と「マイカテゴリー」（編集可能）の2列に分割。マイカテゴリーの選択肢にはGoogle取得カテゴリー（Gemini）も含まれるため、既に登場したGeminiラベルをそのままマイカテゴリーとして選ぶこともできる
- [x] カテゴリー比率チャートをGoogle連動／マイカテゴリーで切り替えられるようにしたい（2026-07-21実装）
  - カテゴリー比率カードの右上にトグルボタンを追加し、集計軸をワンクリックで切り替え可能に（両方同時表示ではなく切り替え式を採用。ユーザーが「両方でもいいが切り替えの方が嬉しい」と回答したため）
  - Google連動カテゴリーはラベル数が青天井になりうるため、件数上位8件のみ個別スライスにし、残りは「その他（上位8件以外）」に集約（一覧・フィルター・エクスポートには影響しない、チャート表示のみの措置）
  - 都道府県×カテゴリーのクロス集計は、CSVエクスポート＋Excelピボットテーブルで対応する運用のため、アプリ内には実装しない（ユーザー確認済み）
- [x] 一覧のカテゴリー抽出（フィルター）と統計カードの最多カテゴリーも、Google連動／マイカテゴリー両対応にしてほしい（2026-07-21実装）
  - フィルターの「カテゴリー」ドロップダウンを「Google連動カテゴリー」用と「マイカテゴリー」用の2つに分割（`filter-category-google`/`filter-category-my`）。両方指定した場合はAND条件。マイカテゴリー側は「未設定」（上書きしていない行）でも絞り込める
  - 統計カードの「最多カテゴリー」も同様に2枚に分割（Google連動／マイカテゴリー）。マイカテゴリー側は未設定行を除いて集計するため、実際に上書きされた行だけを見た「よく使うマイカテゴリー」が分かる
  - 「両方表示するか、円グラフ同様に切り替えられるようにするか」という提案に対しては、常時2枚同時表示を採用（フィルター・統計カードはスペース効率と即時比較のしやすさを優先。円グラフのトグル切り替えは、スペースが限られる単一チャート表示という別の制約から選んだ設計であり、あえて統一しなかった）
- [x] マイカテゴリー列が「未設定」ばかりで見づらいので、未設定行はGoogle連動カテゴリーと同じ表示にし、上書き済みの行だけ分かるようにしてほしい（2026-07-21実装）
  - 未設定行は「未設定（〇〇のまま）」という説明文をやめ、Google連動カテゴリー列と同じカテゴリー名をそのまま表示するように変更（見た目上は2列がほぼ同じに見えるのが正しい状態）
  - 上書き済みの行にだけ✨バッジを表示（Google連動カテゴリー列でGeminiの生ラベルを示すのと同じバッジ意匠を、こちらは「マイカテゴリーで上書き中」の意味で流用）。ホバーで上書き後の値をツールチップ確認できる
- [x] Google連動カテゴリーが実データを持てるようになったので、マイカテゴリーの選択肢から標準12種を外し、完全にユーザー定義にしたい（2026-07-21実装）
  - マイカテゴリーの選択肢を「自作のマイカテゴリー」＋「既に登場したGoogle取得カテゴリー（Gemini）」のみに変更し、標準12カテゴリーは新規の選択肢として提供しないようにした
  - 後方互換：過去のインポート等で`myCategory`が既に標準12種のいずれかに設定されている行は、その行の選択肢にだけ例外的に残し、見た目上「勝手にリセットされた」ように見えないようにしている（新規に選び直すことはできない）
- [x] 定期的にGoogle Takeoutからデータを再読み込みしても、自分で編集したカテゴリー（や都道府県）が上書きされず維持されるようにしたい（2026-07-20実装）
  - マイ都道府県／マイカテゴリー軸の追加により構造的に解決：`deduplicatePlaces` は再インポート時、既存レコードの `myPrefecture`/`myCategory` が未設定の場合のみ埋め合わせ、既存の手動編集値は上書きしない
  - 一覧の都道府県／カテゴリー列は `getEffectivePrefecture`/`getEffectiveCategory`（マイ軸があればそちらを優先）で表示・フィルター・グラフ集計する
  - 残課題：重複排除ロジック自体の精度（URL/座標/名前ベースの近似マッチ）が引き続き前提となる。同じ場所が「別データ」と誤判定されると、せっかく守った手動編集が引き継がれない

### データ入出力
- [x] Googleドライブからダウンロードしたjsonを読み込めるようにしたい（実質達成済みとして2026-07-25にチェック）
  - Drive連携（フェーズ2）の自動読み込み（`loadFromDrive`）、および通常のJSONドラッグ＆ドロップ／ファイル選択（`parseAppBackupJSON`）のどちらでも、Driveからダウンロードしたバックアップjsonを問題なく読み込める。項目追加時点ではDrive連携が未実装だったため`[ ]`のまま残っていたチェック漏れ
- [x] 毎回ファイルを読み込まなくても、前回読み込んだデータをそのまま見られるようにしたい（永続化）（実質達成済みとして2026-07-26にチェック）
  - ローカルキャッシュ（フェーズ3・IndexedDB、2026-07-25実装）で同一端末ならリロードしても自動復元、Googleドライブ連携（フェーズ2）で端末をまたいだ保存・読み込みも可能。項目追加時点ではどちらも未実装だったため`[ ]`のまま残っていたチェック漏れ
- [x] アプリ内にGoogle Takeoutへ直接アクセスできるボタン（リンク）を新設する（2026-07-20実装）
  - アップロードエリアに「Google Takeoutでデータを取得する」リンクを追加。新しいタブで `https://takeout.google.com/settings/takeout` を開く

### 一覧・操作性
- [x] 整理した一覧から、修正したいクチコミを選んだら地図上で確認できるようにしたい（2026-07-20実装）
  - **方針を修正**：Googleマップへの外部遷移ではなく、一覧の「操作」列の地図ピンアイコンをクリックした時に、地図の中心移動・ポップアップ表示に加えて**インタラクティブマップの位置までスムーズスクロール**するようにした（テーブルが上・マップが下のレイアウトのため）
- [x] Takeoutでエクスポートされない／漏れるクチコミを手動で追加・編集できるようにしたい（2026-07-20実装）
  - 「ログを手動で追加」モーダル（`addManualPlace`/`updateManualPlaceFields`）。緯度経度は`35.6586, 139.7454`形式のペーストに対応（`parseCoordinatePair`）、住所から都道府県・カテゴリーは通常のインポートと同じロジックで自動判定
  - 手動入力エントリのみ一覧に編集アイコンを表示し、モーダルを再オープンして再編集可能。まとめて直したい場合向けにCSVエクスポート／インポートにも対応（`exportManualCSV`/`importManualCSV`、IDが一致する行は上書き）
  - **付随して見つかった不具合を修正**：地図中心表示ボタンを追加した際に重複した`class`属性（`class="btn" ... class="btn-locate"`）とセレクタの曖昧さ（`querySelector("button")`が最初の1つしか拾わない）が原因で、編集ボタン追加後に地図中心表示が壊れる可能性があった。専用クラス（`.btn-locate`/`.btn-edit-manual`）で確実に紐付くよう修正し、手動追加→編集→保存後も地図ボタンが正しく機能することを確認済み
- [x] 最終更新日で範囲指定して絞り込みたい（2026-07-20実装）
  - `filter-date-from`/`filter-date-to`（`<input type="date">`）と`matchesDateRange`関数で実装。片方のみ指定でも以降/以前で絞り込み可能
- [x] 削除したスポットがTakeoutの再インポートで復活しないようにしたい（2026-07-26実装）
  - ユーザーからの確認依頼：一覧から削除しても、Takeoutは現在保存中/投稿中の全件を毎回書き出すため、次に再インポートすると同じスポットが復活してしまう不具合を確認。ゴミ箱方式（`deletedPlaces`）で解消（詳細は2.1節・4節）
  - 実装中にユーザーから「間違えて削除する人もいるはずなので復旧機能も」という追加要望があり、ゴミ箱からの「復元する」／「完全に削除」も合わせて実装。回帰テストを`tests/buildPlaceMatchKey.test.js`に追加
- [x] 閉店・削除済みでGoogle側の情報が失われたスポット（「不明なスポット」）を、取り込み時に削除するか確認したい（2026-07-26実装）
  - Takeout等のロバスト抽出パーサーが名前を取得できなかった項目（`extractNameRobustly`が`"不明なスポット"`にフォールバックするケース）を取り込み直後にスキャンし、確認モーダルでチェックを入れたものだけ削除（上記のゴミ箱経由）できるようにした
- [x] 一覧画面（ダッシュボード表示中）からもGoogle Takeoutへ直接遷移できるボタンがほしい（2026-07-26実装）
  - ヘッダーの「ファイルを追加読み込み」ボタンの隣に「Google Takeoutを開く」リンクを追加
- [x] 一覧を開いたときに自分の更新内容がすぐ確認できるよう、最終更新日順をデフォルトにしたい（2026-07-26実装）
  - デフォルトソートを最終更新日の降順に変更。あわせてソート矢印アイコンが一度も切り替わっていなかった不具合も発見・修正（詳細は2.3節）
  - **訂正（同日中に2回修正）**：実装当初は「最終更新日」の紐付け先を`publishTime`→`updateTime`に変更したが、その後ユーザーからの指摘で「Google Takeoutは真の初回投稿日を出力せず`publishTime`が実質の最終更新日である」ことが判明し、`publishTime`基準に戻した。最終的な状態・経緯は4節「`publishTime`／`updateTime`の2軸日付は設計ミスだった」を参照
- [x] 更新後にDriveへの保存を忘れることがあるので、何か対策がなかったか（2026-07-26追加実装）
  - `beforeunload`警告（2026-07-24実装済み）に加えて、作業中も常時見える「未保存の変更があります」バッジをヘッダーに追加（詳細は4節）
- [x] ボタン名の見直し（2026-07-26実装）：ユーザーからの指摘を受けて2件変更
  - **「クチコミを手動で追加」→「ログを手動で追加」**：アプリ名をSpotlogに変えたのを機に、「本当にGoogleマップ本体に直接投稿されるように見えないか」という懸念を解消する狙い。ボタン名を`Spotlog`の「ログ」に統一し、モーダル本文にも「このアプリ内だけの記録で、Googleマップ本体には反映されない」旨を明記した
  - **「カテゴリーをGeminiで取得」「リンク・緯度経度をGeminiで取得」「スポット情報をGeminiで検索」→すべて「〜をGeminiで調べる」に統一**：実際にはどれも「プロンプト生成→Geminiに貼り付けて実行→結果を貼り戻す」という手動の3〜4ステップであり、アプリ自身が自動で取得・検索するわけではないため、動詞が「取得」「検索」で割れていたのを「調べる」に統一。あわせて「カテゴリーをGeminiで取得」は**Google連動カテゴリーとマイカテゴリーのどちらを指すか紛らわしい**という指摘も受け、「Google連動カテゴリーをGeminiで調べる」と明示するように変更（モーダルタイトルは元々明示されていたが、ヘッダーボタン側だけ省略されていた）
- [x] 実データ（900件超）投入後に見つかったレイアウトの作り込み3点（2026-07-27実装）
  - **`.hero-actions`（Drive連携カード）の下がスカスカに見える**：`.hero-card`（OAuth審査用の要約カード、2026-07-26実装）が文章量で自然と背が高くなる一方、`.hero-actions`はDrive連携ボタン+トグルだけで中身が少なく、`.hero-row`が`align-items: flex-start`だったため、右カラムの下に何も無い余白がそのまま露出して見えていた。`.hero-card`にも同じカード背景（`.glass-card`相当の背景・枠線・角丸・padding）を与え、`.hero-row`を`align-items: stretch`に変更して両カードを同じ高さまでストレッチ、`.hero-actions`側は`display:flex; align-items:center;`で中身を縦センタリングすることで、余白が「枠の外」ではなく「カードの中の余白」に見えるようにした
  - **モバイルで「ファイルを追加読み込み」〜「リセット」の操作メニューが縦に長く伸びる**：`#header-actions`の中身を`#toolbar-buttons`（既存の`.action-buttons`）として括り出し、768px以下でのみ表示される「操作メニュー」トグルボタン（`#toolbar-toggle`）を追加。モバイルでは既定で折りたたみ、タップで展開できるようにした（デスクトップでは`.toolbar-toggle`自体を非表示のまま、従来通り常時展開）。
    - **付随して見つかった不具合を修正**：`showDashboard()`等が`#header-actions`（外側のラッパー）に対して`style.display = "flex"`を設定していたため、中の「操作メニュー」トグルボタンと`#toolbar-buttons`が横並びになってしまっていた（後述の折りたたみ機能実装時に発覚）。`#header-actions`は単純な`block`ラッパーに役割変更し、`flex`表示は内側の`#toolbar-buttons`（`.action-buttons`クラス）側に残すことで解消
  - **「スポット一覧」タイトルの右に絞り込み欄が並び、スマホで日付範囲入力がカードからはみ出す**：`.card-header`（タイトルと絞り込み行の親）に`flex-wrap: wrap`を追加し、`.card-title + .filter-row`に`flex: 1 1 100%`を指定。タイトルの次に来る絞り込み行が常にカード全幅で新しい行から始まるようにし、他の`.card-header`利用箇所（カテゴリー比率チャートの軸トグル等、絞り込み行を持たないもの）には影響しないよう隣接セレクタで限定した
  - 動作確認：`node --check`・`npm test`（105件）はパス。デスクトップ（800px幅）・モバイル（375px幅）の両方で3点とも実機確認済み（モバイルでの折りたたみ→展開の往復、絞り込み行のカード内収まり、Drive連携カードの見た目を含む）
- [x] 実データ（900件超）投入後に見つかった運用面の作り込み6点（2026-07-28実装）：ユーザーが実データを日常的に使う中で溜まったフィードバックへの対応
  - **ツールバーの並び順を実際の登録手順に合わせて再編**：従来はボタンの並びが実際の作業フロー（Takeoutを開く→取り込み→分類→（漏れの）手動追加・座標補完→設定/エクスポート等の管理系）と一致しておらず、使う頻度・順序と見た目の並びがバラバラだった。①メイン（Google Takeoutを開く／ファイルを追加読み込み／Google連動カテゴリーをGeminiで調べる）②サブ（ログを手動で追加／スポット情報をGeminiで調べる／リンク・緯度経度をGeminiで調べる）③管理系（マイカテゴリー設定／CSVエクスポート／JSONエクスポート／緯度経度 要確認／削除済みスポット／リセット、頻度が低いので最後）の3グループに再編し、グループ間に区切り線を追加（`.action-button-group`）
  - **デスクトップで`.hero-actions`（Drive連携カード）下部の余白にツールバーを統合**：2026-07-27修正でカードの高さは揃えたが、Drive連携ボタン+トグルだけでは中身が少なく、カード下半分の余白は残ったままだった。`#header-actions`（ツールバー）を`.hero-actions`カードの内部（`#drive-sync`の下）へDOM移動し、`.hero-actions`を`flex-direction: column`にすることでDrive連携ボタン群とツールバーが同じカード内に縦に並ぶようにした。モバイルでの折りたたみ挙動（`.toolbar-toggle`）は影響を受けない（CSSのメディアクエリ側の制御のみで動くため）
  - **モバイルでのhero-card（Spotlog説明〜プライバシーポリシー）の表示優先順位を後回しに**：このカードは実際にはGoogleのOAuth本番審査対策として「ホームページにアプリの目的説明を常時表示する」ために作った部分で（3節参照）、過去に同じ理由で「閉じたdetails要素やdisplay:noneでの非表示化はNG」と判断した経緯がある（3節「公開説明を機械判定しやすい構造へ再編成」参照）。この制約を踏まえ、内容・表示状態（display:none不使用）はそのまま維持しつつ、768px以下でのみ`.hero-actions`（実際の操作）に`order: 1`、`.hero-card`（説明カード）に`order: 2`を指定して表示順を入れ替え、あわせて`.hero-card`のフォントサイズ・paddingを縮小した。「毎回開くたびに説明文が操作の前に立ちはだかる」というユーザーの不満と、審査対策上の制約の両方を満たす
  - **一覧テーブルのページネーション（50件区切り）を追加**：実データが900件を超え、一覧テーブルを一括描画すること自体が重く、かつ「開いた瞬間に全件が画面に並ぶ」こと自体が運用上好ましくないという指摘を受けて対応。`TABLE_PAGE_SIZE`（50件）ごとに`renderTable`が描画対象をスライスし、テーブル下に前へ/次への操作と「◯件中◯〜◯件（◯/◯ページ）」を表示する。絞り込み・地図・グラフ・統計カードは引き続き絞り込み結果全体を対象にし、ページングの対象はテーブルの描画のみ。検索・フィルター・ソートの変更時は1ページ目に戻すが、行の編集（マイ都道府県／マイカテゴリーの上書き等）で再描画される場合は現在のページを維持する（`filterAndRenderFromPage1`と素の`filterAndRender`を使い分け）
  - **Geminiプロンプトの「コピー」ボタンにコピー成功時のフィードバックを追加**：`navigator.clipboard.writeText`自体は一瞬で終わり見た目に変化が無いため、特にデータ量が多く動作が重い時ほど「コピーできたか」が分かりづらいという指摘を受けて対応。カテゴリー・リンク緯度経度・スポット検索の3つの「コピー」ボタンに、クリック後1.5秒だけ「✓ コピーしました」表示へ切り替わる`showCopySuccess`を追加。あわせて、Geminiへ移動する手間を減らすため各コピーボタンの隣に新しいタブで`gemini.google.com`を開く「Geminiを開く」リンクを追加（外部AIからのレビューで提案された改善点）
  - **Geminiの回答（JSON）のパースを、コードフェンス無しで前後に説明文が付く場合にも対応できるよう堅牢化**：従来は\`\`\`json フェンスを剥がした上で直接`JSON.parse`するだけだったため、Geminiが「こちらが結果です」等の説明文をフェンス無しで前後に付けて返すとパースに失敗していた（外部AIからのレビューで指摘）。直接パースに失敗した場合、テキスト中の最初の`[`から最後の`]`までを切り出して再度パースを試みるフォールバックを共通ヘルパー`extractJSONArrayFromGeminiResponse`として切り出し、カテゴリー・リンク緯度経度・スポット検索の3つのGemini回答パーサーで共通化した
  - 動作確認：`node --check`・`npm test`（108件）はパス。ブラウザ実機（静的サーバー経由）で、60件のCSVをサンプルデータ15件に追加取り込みして75件にし、ページネーションの前へ/次へ・件数表示・行編集時のページ維持／フィルター変更時のページ1復帰・コピー成功表示（1.5秒で復帰）・座標ハルシネーション時の要確認フラグ登録から「確認OK」でのフラグ解除まで一通り確認済み。モバイル幅（375px）でのhero-card/hero-actionsの表示順入れ替え・横スクロール無しも確認済み

### モバイルレイアウトの作り込み
- [x] スポット一覧テーブル（8列）のスマホ向けレイアウトを設計する（2026-07-20実装）
  - 768px以下では、テーブルではなく「1件＝1枚のカード」形式（ラベル＋値を縦積み、スポット名がカードタイトル）に切り替え。横スクロール依存を解消し、住所・レビューは折り返し表示に変更
  - 地図・グラフ部分（3カラム→1カラム化）は既存対応のままで問題なし

### 運用・品質面での提案（Claudeより）
- [x] リセット操作の前に自動でJSONバックアップを促す導線を追加する（誤操作で全データを失わないように）（2026-07-20実装）
  - `resetApp()` でデータが1件以上ある場合、リセット確認の前に「先にJSONバックアップをダウンロードしますか？」の確認を追加
  - **付随して見つかった不具合を修正**：エクスポートしたJSONを再アップロードしても、座標（キー名不一致で `coordinates.latitude/longitude` が読めていなかった）と手動編集したカテゴリー・都道府県（常に自動判定で再計算されていた）が復元されないバグがあった。専用の復元パーサー（`parseAppBackupJSON`、`categoryKey` の有無で自社エクスポート形式を検出）を追加し、保存済みの値をそのまま復元するように修正
- [x] `classifyCategory` の分類ロジックに対して、代表的なケース（ラーメン店、ホテル「〜イン」の誤判定除外など）の簡単なテストを用意する（2026-07-20実装）
  - `tests/classifyCategory.test.js`、`tests/parseAppBackupJSON.test.js` を追加。以降 `buildManualPlaceFields.test.js`／`matchesDateRange.test.js`／`parseCoordinatePair.test.js`／`parseCSVRows.test.js`／`parseAppCSVBackup.test.js` も追加（Node標準の `node --test`、追加npm依存なし）。`npm test` で実行可能
- [x] CSVインポートが「ぐるぐる回ったまま固まって戻ってこない」不具合を修正（2026-07-20実装）
  - 原因：クチコミ本文が複数行（引用符内改行）にまたがるCSVで、行を先に改行分割してから解析していたためレコードが分裂し `undefined` が混入 → 重複排除処理が例外を投げる → ローディング画面を閉じる処理まで届かず無限にスピナーが回っていた
  - CSV全体を引用符を認識しながら一括パースする方式（`parseCSVRows`）に変更し、複数行のレビュー本文も1レコードとして正しく読み込むように修正。列が足りない行は`undefined`ではなく空文字にフォールバック（`csvField`）。`handleFiles`本体を`try/finally`で保護し、想定外のエラーが起きても必ずローディング画面が閉じるようにした
- [x] CSVエクスポートを最初のアップロード画面から再読み込みすると、手動入力エントリの編集アイコンが消える（マイカテゴリー等の上書きも失われる）不具合を修正（2026-07-20実装）
  - 原因：`parseCSVData`（汎用CSV取り込み）が「データソース」列を一切読んでおらず、再インポートしたレコードは常に`source: "CSVインポート"`として扱われていた
  - JSONバックアップ復元と同じ考え方で、CSVヘッダーの「データソース」列の有無から自社の全件エクスポート形式を検出し、専用の復元処理（`parseAppCSVBackup`）で`source`・都道府県・マイ都道府県・カテゴリー・マイカテゴリーをそのまま復元するように修正。カテゴリー名→キーの逆引きで変換するため、未登録の独自マイカテゴリーだけは復元できない制約が残る（4節に記載）
- [x] 都道府県の自動判定が、店名に他都道府県の地名が入っていると誤判定する不具合を修正（2026-07-21実装）
  - ユーザー報告：「福岡市博多区にある『北海道ラーメン』という店が、店名の『北海道』のせいで都道府県が北海道と登録されてしまう」
  - 原因：`extractPrefecture`が都道府県を1件ずつ順番にチェックする際、各都道府県について「住所に含まれるか」と「店名に含まれるか」を同時にチェックしていたため、`PREFECTURES`配列の先頭にある「北海道」という都道府県名がたまたま店名に含まれているだけで、本来の住所（福岡県）を一度も確認しないまま北海道と誤判定していた
  - 住所を全都道府県分チェックし終えるまでは店名を一切見ないように順序を修正（住所 → 座標 → 店名、の優先順位を明確化）。店名からの推測は、住所にも座標にも都道府県情報が無い場合の最終手段としてのみ使う。回帰テストを`tests/extractPrefecture.test.js`に追加
- [x] Googleマップ側でレビュー・評価が編集された場合に、再インポートでその変更に追従できるようにしたい（2026-07-22実装）
  - ユーザーからの相談：Google Takeoutは直近600件程度しかエクスポートできない制限があるため、このアプリでデータを蓄積し続けること自体に価値がある。一方でGoogle側でレビュー本文や評価を後から編集すると、再エクスポート時に最終更新日（`updateTime`）が変わって返ってくるため、単純な「空欄埋めのみ」のマージでは編集内容に追従できない、という課題
  - 「完璧な判定方法は個人開発の範囲では追求しすぎず、まず妥当な方針で実装し、要望が出たら調整する」という方針をユーザーと合意
  - `deduplicatePlaces`のマージ処理を変更：既存レコードより取り込みデータの`updateTime`が新しい場合は、コメント・評価・住所・都道府県・Google連動カテゴリー（`googleCategoryRaw`/`category`）を新データで追従上書きする。同じか古い場合は従来通り空欄埋めのみ。手動入力レコードは対象外、マイ都道府県／マイカテゴリーは`updateTime`に関わらず常にユーザー編集を保護（未設定時のみ埋め合わせ）。詳細は2.1節・4節
  - 回帰テストを`tests/deduplicatePlaces.test.js`に追加（追従上書き／据え置き／手動入力の除外／マイ軸保護／別レコード扱いのケースをカバー）

## 6. 実装の進め方（フェーズ）

**方針：機能の作りこみを先に、PWA化（特にオフライン対応部分）は後回しにする**

- PWA化はマニフェスト＋Service Workerを既存コードにほぼ手を入れず後付けできるため、先にやる必然性が薄い
- オフラインキャッシュの設計（端末ごとのON/OFF等）はGoogle Drive連携（認証・同期方式）が固まって初めて具体化するため、Drive連携より先にやると二度手間になりやすい
- 「ブラウザUIなしで単体ウィンドウ起動する」体験だけ早めに試したい場合、Service Workerなしでマニフェストだけ先に用意するのはコストが低く問題ない

| フェーズ | 内容 | 状況 |
|---|---|---|
| 1 | 認証・同期に依存しないロジック追加（マイカテゴリー/マイ都道府県の2軸分離、Geminiプロンプト機能、Takeoutボタン、クチコミ→Googleマップ遷移、リセット前バックアップ、クチコミ手動追加・編集、日付範囲フィルター、分類ロジックのテスト、モバイル一覧のカード化） | Takeoutボタン／クチコミ地図遷移／リセット前バックアップ（＋復元バグ修正）／分類テスト／モバイルカード化／マイカテゴリー・マイ都道府県2軸分離／クチコミ手動追加・編集（CSV一括編集込み）／日付範囲フィルター／Google連動カテゴリーのGeminiプロンプト方式取得（生ラベルそのまま使用に方式転換込み）／一覧のGoogle連動・マイカテゴリー2列表示／カテゴリー比率チャートの軸切り替え／都道府県自動判定の店名誤判定バグ修正は完了（2026-07-20〜21）。残: Google連動カテゴリーのGoogle Places API方式（bの代替案a、未着手・優先度低） |
| 2 | Google Drive連携（OAuth、保存・読み込み） | 完了（2026-07-24）。本番URL（GitHub Pages）で連携→保存→別セッションでの連携→読み込みまで実機確認済み |
| 3 | オフラインキャッシュ設計（IndexedDB、端末ごとのON/OFF設定） | 完了（2026-07-25）。Driveアクセストークンの永続化は見送り（詳細は2.5節） |
| 4 | PWA本実装（マニフェスト＋Service Worker） | インストール可能化のみ完了（2026-07-26）。フルオフライン対応（CDN依存の解消）は未着手 |

**Google Drive連携（フェーズ2）実装状況（2026-07-24）**
- 準備完了：GitHub Pages有効化済み（公開URL: https://numacchi14-cpu.github.io/google-maps-dashboard/ 、200を確認済み）、Google Cloud ConsoleでOAuthクライアント作成済み（アプリ名は"Google"を含められないため「G-MAP DashBoard」で登録、テストユーザー・drive.fileスコープとも設定済み、承認済みJavaScript生成元に上記Pages URLを設定）
- コード側のOAuth連携（Google Identity Services導入、トークン取得、Drive REST APIでの保存/読み込み）を実装済み：
  - `index.html`：`<script src="https://accounts.google.com/gsi/client">` を追加。ヘッダーに常時表示の`#drive-sync`ブロック（「Googleドライブと連携」ボタン／未連携時は非表示の「Driveに保存」ボタン／ステータステキスト）を新設。アップロード前でも押せる位置に配置し、連携直後にDrive上の既存データを読み込めるようにしている
  - `app.js`：GISのimplicitトークンクライアント（`getDriveTokenClient`/`connectGoogleDrive`）でdrive.fileスコープのアクセストークンを取得。`loadFromDrive`はファイル名`g-map-dashboard-backup.json`をDrive内で検索→`alt=media`で取得→既存の`parseAppBackupJSON`＋`deduplicatePlaces`経由でローカルの`places`にマージ（ファイルインポートと同じ安全なマージ経路を再利用し、ローカルの未保存編集を上書きしない）。`saveToDrive`はJSONバックアップと全く同じ構造（`buildBackupJSONPayload`——`exportJSON`用の生成処理を共通関数として切り出し）をmultipart uploadでDriveへ作成/更新（`driveFileId`があればPATCH、なければPOST）
  - アクセストークンはメモリ上のみ（`driveAccessToken`）、リロードで消える想定通りの挙動（フェーズ3実装時に永続化を検討したが見送り。理由は2.5節）
  - 動作確認：`node --check`・既存テスト62件はパス。ローカルdevサーバー＋Playwrightでページ読み込み・サンプルデータ表示・ヘッダーUIの見た目とコンソールエラー無しを確認済み
  - **本番URLでの実機確認も完了**（2026-07-24）：コミット・push→GitHub Pagesへ自動デプロイ→「連携」→「保存」→別ブラウザ（シークレットウィンドウ）で「連携」→自動読み込み、まで一連の動作を確認済み
    - 途中、保存時に403エラーが発生。原因はGoogle Cloud Consoleで**Drive APIそのものが未有効化**だったこと（OAuthクライアント登録・スコープ設定とは別に、「APIとサービス」→「有効なAPIとサービス」でGoogle Drive API自体を明示的に有効化する必要がある）。有効化後は問題なく保存・読み込みとも成功
- 残タスク：
  - Google連動カテゴリーのPlaces API方式（選択肢a）は引き続き後回し（優先度低、合意済み）

**フェーズ3（ローカルオフラインキャッシュ）実装状況（2026-07-25）**
- 完了：IndexedDB（`g-map-dashboard-cache`、単一オブジェクトストア・単一レコード）に`places`一式をキャッシュし、次回起動時に自動復元する仕組みを実装。詳細・設計判断は4節「状態は基本メモリ上のみ、ただしローカルキャッシュ（IndexedDB）で継続可能に」を参照
- ヘッダーの「Googleドライブと連携」ボタン群の隣に「このデバイスにデータを保存」トグル（初期値ON）を追加（`index.html`の`#drive-sync`ブロック内、新規モーダルは作らず既存の常時表示ボタン群に統合）
- Driveアクセストークンの永続化は、このフェーズで検討した上で見送り（2.5節に理由を記載）。フェーズ3は「アプリデータのローカル継続」のみを実装し、「Drive連携状態の継続」は引き続き毎回「連携」ボタンを押す運用のまま
- 動作確認：`node --check`・`npm test`（100件）はパス。IndexedDB/localStorage部分はブラウザAPI依存でNode側のユニットテストが難しいため（Drive連携自体も同様の理由でユニットテスト無し）、純粋ロジック部分（`shouldScheduleLocalCacheWrite`）のみ`tests/localCache.test.js`でカバーし、実際の永続化挙動（トグルON/OFF、リロード後の復元、リセット時のキャッシュ削除、IndexedDB不可環境でのフォールバック）はブラウザ実機での確認が必要
- 残タスク：フェーズ4（PWA本実装）は未着手のまま

**フェーズ4（PWA本実装）実装状況（2026-07-26）**
- 目的を「ブラウザUI無しでホーム画面から起動できるインストール可能な体験」に絞り、フルオフライン対応（4節「外部CDN依存」の解消）は今回のスコープ外とした（Leaflet/Chart.js/Lucide/Googleフォントは引き続きCDN読み込みのため、地図・グラフ・アイコン表示にはネット接続が必要なまま）
- `manifest.json`を新設（`name`/`short_name`はSpotlog、`display: standalone`、`background_color`/`theme_color`は`--bg-primary`と同じ`#0b0f19`、`start_url`/`scope`は`/`）。アイコンは`icons/icon-192.png`・`icons/icon-512.png`・`icons/icon-512-maskable.png`の3種（OAuthブランディング用に作った地図ピンのロゴをPillowスクリプトで複数サイズ・マスカブル対応版に書き出して流用。マスカブル版は角丸なし全面塗りつぶし＋ピンを安全領域に収まるよう縮小）
- `index.html`に`<link rel="manifest">`・favicon・`apple-touch-icon`・`theme-color`等のPWA関連タグを追加
- `sw.js`を新設。キャッシュ対象はアプリ本体のシェル（`index.html`/`app.js`/`style.css`/`manifest.json`/`privacy.html`/`help.html`/アイコン3種）のみで、CDNリソース（クロスオリジン）は一切インターセプトしない設計。ページ遷移（navigation）はネットワーク優先＋オフライン時のみキャッシュにフォールバック、静的アセットはキャッシュ優先＋無ければネットワークから取得してキャッシュに追加、という単純な2方式。`app.js`のDOMContentLoaded内で`navigator.serviceWorker.register("/sw.js")`を実行して登録
- 動作確認：`node --check`・`npm test`（104件）はパス。ブラウザ実機で`manifest.json`の取得・3アイコンの200応答・Service Workerの`active`状態・シェルファイルのキャッシュ格納（`caches.open("spotlog-shell-v1")`で10ファイル確認）・コンソールエラー無し、を確認済み
- 残タスク：フルオフライン対応（Leaflet/Chart.js/Lucide/フォントのローカル同梱）、Google Playストア配布（PWAのTWA化、任意・優先度低）

### 公開範囲拡大・収益化・配布形態（2026-07-24 優先順位合意）

GitHub Pagesで不特定多数に公開すること自体はGitHubの規約上問題なし（個人ツールへの広告掲載も含めて許容範囲、というのが確認済みの認識）。広告収益化は実利目的というより**知的好奇心での実験**という位置づけ。以下の順で進めることに合意：

1. **プライバシーポリシーページ＋簡易マニュアル/ヘルプページの作成**（2026-07-24実装済み）
   - プライバシーポリシーは②（OAuth本番公開の審査）と③（AdSense審査）**両方に共通の前提条件**なので、先に片付けておくと二度手間にならない
   - マニュアルは「初めて触る人でもGoogle Takeoutでのデータ取得〜読み込みまでできる」くらいの簡易な内容を想定
   - `privacy.html`（運営者情報、クライアントサイド完結の説明、Drive連携の`drive.file`スコープの範囲、Cookie/広告は現状未使用、CDN経由の外部サービス一覧）と`help.html`（Takeout取得手順、読み込み方、主要機能、よくあるQ&A）を新規作成。既存の`style.css`をそのまま読み込んで見た目を統一（`.doc-container`等の新規クラスを追加）
   - `index.html`にフッター（使い方ガイド／プライバシーポリシー／GitHubへのリンク）を追加し、アプリ本体からも実際に辿れるようにした（OAuth審査はプライバシーポリシーがアプリ内からリンクされていることを要求するため）
   - **付随して見つかった不具合を修正**：`.dashboard-grid`が`opacity:0`のみで`display:none`になっておらず、データ未読み込み時でも透明なまま高さ分のレイアウト領域を専有していた（フッター追加で空白として顕在化）。`display: none`をベースに追加し、`showDashboard()`が明示的に`display:"grid"`をセットする既存の見せ方はそのまま維持
   - [x] 使い方ガイドへの導線がフッターのみで、初めて訪れた人が見つけにくい問題を修正（2026-07-24実装）：アップロード画面上部（「ファイルを選択する」ボタンの下、「Google Takeoutでデータを取得する」リンクの上）に「はじめての方はこちら：使い方ガイド」ボタンを追加。フッターの導線は既存ユーザー向けにそのまま残した
   - [x] クチコミが600件を超える場合の迂回方法を使い方ガイドに追記（2026-07-25実装）：`help.html`の「よくある質問」に「クチコミをつけたお店が600件を超えています。どうすればいいですか？」を新設。「スポット情報をGeminiで検索」（うろ覚えの手がかりから最大30件まとめて候補検索・追加）→「クチコミを手動で追加」モーダル内のCSVエクスポート／インポート（評価・コメント・投稿日をExcel等でまとめて後入力）という、Takeoutの上限に依存しない組み合わせ運用を案内する内容。ユーザー自身が実践している「Google Mapsのクチコミ画面を遡ってコピペ→Claude Codeで整形→CSV貼り付け」というワークフローもこれと同じ迂回策の発展形（詳細は運用メモ、アプリ側には未実装）
2. **フェーズ3＋4（オフラインキャッシュ／PWA化）**：自分の日常利用の質を上げる。PWA化は④のストア配布の技術的前提にもなる（フェーズ3は2026-07-25完了、フェーズ4は未着手）
   - **アプリ名を「Spotlog」に変更、独自ドメインへ移行する方針を決定（2026-07-26検討開始）**：アプリ名が「Google Maps マイプレイス ダッシュボード」のままだと、次項③のOAuth本番審査で「Google」を含む名称として引っかかりやすい懸念があった（OAuthクライアント自体は元々`G-MAP DashBoard`という代替名で登録済み。2.5節参照）ため、アプリ名自体を**Spotlog**に変更する方針で合意。「Googleマップ前提のツールである」ことは商品名からは外し、アプリ内の説明文（`index.html`のヘッダー副題・`privacy.html`冒頭）で明示する形にした（`index.html`/`privacy.html`/`help.html`のタイトル・見出しを更新済み）
   - 併せて、ユーザーが個人ドメイン`num-ish.com`をCloudflare Registrarで取得済み（複数の個人アプリ・ブログをまとめる将来構想の一部）。このアプリはサブドメイン`spotlog.num-ish.com`に割り当てる方針。ドメイン移行は③のOAuth審査より**先に**行うことで合意（Search Consoleのドメイン所有権確認・OAuth同意画面の設定は審査後に変更すると二度手間になるため）
   - 実施手順：①CloudflareにDNSレコード（`spotlog` CNAME → `numacchi14-cpu.github.io`、まずはDNSのみ/proxy off でGitHub側のHTTPS証明書発行を通す）を追加 → ②GitHub Pages側のカスタムドメイン設定（`CNAME`ファイル or リポジトリ設定）を`spotlog.num-ish.com`に変更 → ③Google Cloud ConsoleのOAuthクライアント表示名・承認済みJavaScript生成元を更新 → ④新ドメインでSearch Console所有権確認 → ⑤本番審査申請（次項③）。GitHubリポジトリ名（`google-maps-dashboard`）自体の変更は任意・未定（コード内にリポジトリ名のハードコードは無いため、名前空間の整理以外に必須の理由はない）
   - **①〜③完了（2026-07-26）**：CloudflareのDNS（DNSのみ）反映を確認後、リポジトリに`CNAME`ファイル（`spotlog.num-ish.com`）を追加してGitHub Pagesのカスタムドメインを切り替え、HTTPS証明書発行済みを実機で確認。OAuthクライアントの「承認済みのJavaScript生成元」に`https://spotlog.num-ish.com`を追加（`https://numacchi14-cpu.github.io`は動作確認期間として残置）し、新ドメイン上でのGoogleドライブ連携（認証・保存・読み込み）を実機確認済み。クライアント名は「G-MAP DashBoard」のまま（エンドユーザーには非表示のコンソール内ラベルのため変更は任意と判断）。残: ④Search Console所有権確認（`spotlog.num-ish.com`）→⑤本番審査申請
   - **旧GitHub Pages URLの後片付け（2026-07-26）**：GitHub Pagesはカスタムドメイン設定後、旧URL（`https://numacchi14-cpu.github.io/google-maps-dashboard/`）へのアクセスを新ドメインへ自動的にリダイレクトする（実機確認済み。独立したコピーとして表示され続けるわけではない）ため、このアプリのJSが旧オリジン上で実行されることはもう無い。これを踏まえ、OAuthクライアントの「承認済みのJavaScript生成元」から`https://numacchi14-cpu.github.io`を削除（不要になった生成元を残さない、構成のクリーンアップ）
3. **公開範囲を広げる**：Search Consoleでのドメイン所有権確認（`spotlog.num-ish.com`、上記の独自ドメイン移行後）→OAuth同意画面を「テストモード」から本番公開→Google側のアプリ確認審査を申請
   - `drive.file`は「機微(sensitive)」スコープであり「制限付き(restricted)」ではないため、CASAのような有償セキュリティ監査は不要（無料・数日〜数週間程度の審査で完了する想定）
   - **審査準備状況（2026-07-26）**：ロゴ画像を用意（PillowでPythonスクリプト生成。`--accent-gradient`と同じ配色＋アプリ内で使っている地図ピンのモチーフ、512×512px）してCloud Consoleの「ブランディング」にアップロード済み。アプリ名欄は「Spotlog」で正しく保存されていることを実機確認済み
   - **ブランディングの自動チェックで指摘された問題と対応（2026-07-26）**：Cloud Console内の事前チェック（Googleへの正式送信前の自己診断）で以下2点を指摘された
     1. 「ホームページでアプリの目的が説明されていません」：`index.html`のヘッダー副題が一行だけで薄かったため、Spotlogの機能説明とGoogleドライブ連携（`drive.file`スコープ）の使途を明記した段落（`.app-about`）を追加
     2. 「OAuth同意画面のアプリ名『Spotlog』とホームページのアプリ名が一致していません」：Cloud Console側の設定自体は確認済みで問題なし（アプリ名欄は「Spotlog」）だったため、自動クローラーが読み取りやすいようメタタグ（`application-name`/`description`/`og:site_name`/`og:title`/`og:description`）を追加して再検証待ち。原因はクロール側の解釈の可能性が高いが確証はない
     - 対応後は反映まで少し時間がかかる想定のため、デプロイ後にCloud Console側で「もう一度検証」を再実行する運用
   - **ルートドメイン（`num-ish.com`）が空だった件への対応（2026-07-26）**：上記2点の修正後も同じ指摘が続いたため調査したところ、Search Console側のドメイン所有権確認を`num-ish.com`の**ドメインプロパティ**（サブドメインごとではなくドメイン全体）で行っていたため、OAuth同意画面の「承認済みドメイン」が`num-ish.com`（ルート、サブドメイン無し）として登録されていることが判明。一方でCloudflareのDNSには`spotlog`サブドメインのCNAMEしか無く、ルート自体には何のレコードも無かった（`nslookup num-ish.com`が「レコードなし」を返す状態。Cloudflareダッシュボードの推奨事項にも「訪問者はnum-ish.comにアクセスできません」と出ていた）。**Googleの審査クローラーは、ホームページURLだけでなく承認済みドメインのルート自体もチェックしている可能性が高い**と推測し、`num-ish.com`から`https://spotlog.num-ish.com`への301リダイレクトをCloudflareの「ページルール」（`num-ish.com/*` → URLの転送 → `https://spotlog.num-ish.com/$1`、無料プランの3枠のうち1つ使用）で設定。ルート用にダミーのAレコード（`@` → `192.0.2.1`、プロキシ経由/オレンジクラウド）を追加してCloudflareがトラフィックを拾えるようにした上でのリダイレクトのため、実在するサーバーへの接続は発生しない。モバイル回線での実機確認で動作を確認済み（PC側のISP DNSキャッシュは反映に時間がかかったが、Cloudflareの権威DNS（1.1.1.1）へ直接問い合わせると即座に正しい応答が返ることを確認し、設定自体の誤りではなく伝播待ちであると判断できた）
     - **将来のホームページ／ブログ計画との関係**：`num-ish.com`には将来的に複数の個人アプリ・ブログをまとめたホームページを置く構想があるため、今回のリダイレクトはあくまで暫定処置。本物のホームページを用意する際は、このページルールを削除し、ルートのDNSレコードをそのホスティング先に向け直すだけで移行できる（Spotlog側の設定には影響しない）
   - **公開情報・固定ページの本格整備（2026-07-26実装、ChatGPTと相談してまとめた指示書に基づく）**：ユーザーが別途ChatGPTと相談してまとめた「Claude Code向け Spotlog OAuth審査対応・改修指示書」に沿って実施。着手前に既存のOAuth実装（スコープは`drive.file`のみでプロフィール/メール等は要求していないこと、アクセストークンはメモリ上のみでlocalStorage等に保存しないこと、バックアップファイル名・検索方法・上書き方法）と、Gemini連携機能の実際のデータフロー（`app.js`内の`fetch()`はすべてGoogle Drive APIのみが対象で、Gemini関連の3機能——カテゴリー・リンク/緯度経度・スポット検索——はいずれも`navigator.clipboard.writeText()`でプロンプトをコピーするだけの完全手動往復であり、外部への自動送信は無いこと）を調査・報告し、ユーザーの承認を得てから実装した
     - **運営者の実名掲載（2026-07-26、ユーザー承認済み）**：`privacy.html`の運営者表記を匿名（「本アプリの開発者（個人）」）から「菱沼政彦（Spotlog開発・運営者）」に変更。トップページの説明文にも「NUM-ISH運営者の菱沼政彦が個人で開発・運営しています」を追加し、NUM-ISHサイト側の表記と一貫させた。勤務先・役職等は掲載しない方針（[[project_num_ish_brand_site]]参照）
     - **`index.html`のトップ説明（`.app-about`）を拡充**：単一パラグラフから、概要・主な機能一覧・対象ユーザー・Googleドライブ連携の目的（`drive.file`のみ・氏名等は取得しない・データ販売や広告目的での利用はしない）・非公式サービスである旨・固定ページへのリンク（プライバシーポリシー／利用規約／使い方ガイド／お問い合わせ）を含む複数段落に拡張。ログイン不要・データ未読み込みの状態でも常に閲覧できる位置（ヘッダー内、アップロード導線より上）は変更していない
     - **`terms.html`を新規作成**：ログイン不要で閲覧できる独立ページとして、概要・運営者・利用条件・データ責任・バックアップ推奨・禁止事項・サービス変更/停止・外部サービス・無保証・データ消失の責任範囲・知的財産権・規約変更・問い合わせ先を掲載。準拠法・管轄裁判所は指示書の方針により未確定のまま記載しない
     - **`privacy.html`を拡充**（**URL・ファイル名は変更せず**、Cloud Console登録済みの`https://spotlog.num-ish.com/privacy.html`と一致させたまま）：非公式サービスである旨、HTTPS通信・サーバー不在によるデータ保護、データ種別ごとの保存期間（ローカル／Driveバックアップ／アクセストークン）、第三者提供・データ販売・広告利用を行わない旨、Google API Services User Data Policy（Limited Use要件）への準拠、データ削除・連携解除の3手順（①アプリの「リセット」でローカルデータ削除、②Googleドライブで利用者自身がバックアップファイルを削除、③Googleアカウントの「サードパーティ製のアプリとサービスとの接続」から連携解除——3つは別操作であり運営者側で一括削除はできない旨を明記）を追加
     - **`help.html`に「STEP 7: 連携解除・データ削除を行う」を新設**：上記3手順を使い方ガイドの文脈でも案内し、プライバシーポリシーと矛盾しないよう表現を揃えた
     - **変更しなかったもの（指示書の指示通り）**：Google Auth Platformの設定、OAuthクライアントID・スコープ・リダイレクトURI、`spotlog.num-ish.com`のCNAME、GitHub Pages設定、Googleドライブ連携の認証処理コード（`app.js`のOAuth関連部分は今回の一連の編集で無変更）、`privacy.html`のURL自体
     - 動作確認：`node --check`・`npm test`（105件）はパス。ブラウザ実機でトップページ（Drive未連携でも全文閲覧可）・`privacy.html`・`terms.html`（404にならないこと）・`help.html`の表示、コンソールエラー無しを確認済み
     - **文言修正（2026-07-26、Google審査申請後の実機確認で指摘）**：トップページの「データの処理はすべてお使いのブラウザ内だけで完結し、外部のサーバーに送信されることはありません」が、任意のGoogleドライブ連携時にバックアップデータがGoogle Driveへ送信される事実と矛盾する絶対表現だったため、「通常のデータ処理は利用者のブラウザ内で行われ、運営者が管理するサーバーへ送信されることはありません。利用者がGoogle Drive連携を選択した場合に限り、バックアップデータは利用者のブラウザからGoogle Driveへ直接送信されます」に修正。`terms.html`の「Googleアカウントでのログインが必要です」も本人確認と誤解されないよう「Googleアカウント上でSpotlogによるGoogle Driveへのアクセスを許可する操作が必要です。これはSpotlogへのアカウント登録や本人確認を目的とするものではありません」に修正
     - **公開説明を機械判定しやすい構造へ再編成（2026-07-26）**：Googleの審査で同一指摘（ホームページの目的説明不足／アプリ名不一致）が繰り返されたため、トップページの構造自体を見直した。①`<title>`を「Spotlog」の完全一致に変更（従来は説明文が続いていた）、②`<h1>`を`<span class="gradient-text">`によるラップをやめ`<h1>Spotlog</h1>`という単純な構造に変更（グラデーション自体は`.header-title-container h1`セレクタへ直接CSSを移して維持）、③`.app-about`内の説明文に`<h2>Spotlogについて</h2>`・`<h2>Googleドライブ連携の目的</h2>`（Drive連携の利用目的・不使用範囲を箇条書き化）という明示的な見出しを追加、④英語版の目的説明（`<h2 lang="en">Purpose of Google Drive Integration</h2>`と`<p lang="en">`）を追加。認証処理・`app.js`・Google Cloud設定は今回も無変更（`git diff --stat app.js`で差分ゼロを確認）
     - **ファーストビュー内にDrive連携ボタンが収まるようレイアウトを再編成（2026-07-26）**：説明が長くなり、Googleドライブ連携ボタンがファーストビュー外に押し出されていたため、内容を削除せずに構造を分割した。ヘッダーを`.hero-row`（`.hero-card`＝H1・目的1文・Drive連携の要点3行・短い英語説明・プライバシーポリシーへのリンクのみのコンパクトな要約カード／`.hero-actions`＝Drive連携ボタン等の操作）の2カラムに再編成し、デスクトップでは横並び、768px以下では`.hero-row { flex-direction: column }`で縦並び（要約→操作の順）にした。主な機能・対象ユーザー・広告/第三者提供なしの詳細・非公式サービス表示・詳しい英語説明・利用規約/ヘルプ/問い合わせリンクは、削除せず操作エリア（Drive連携ボタン・アップロード画面）より下の`.app-details`セクション（通常表示、`display:none`・タブ・モーダル・閉じた`<details>`には入れない）へ移動。ヘッダー内にあった`header-actions`（データ読み込み後のツールバー）は`<header>`タグの外に出し、border/paddingを`<header>`側ではなく`.app-details`の`border-top`で代替（`header-actions`が`display:none`の間は空の枠が表示されないようにするため）。デスクトップ（800px幅相当）・モバイル（375px幅）双方で、Drive連携ボタンがスクロール無しで見えることを実機確認済み
     - **`.app-details`をdashboard-sectionの後・フッターの前へ移動（2026-07-26）**：通常利用時（データ未読み込み〜読み込み後）に「詳しい説明」セクションがダッシュボードより上に来て既存の操作領域を押し下げていたため、DOM上の位置をdashboard-section直後・footer直前へ移動（内容・見出し・日英の説明・各リンクは変更なし。表示方法も通常表示のまま）。
       - **付随して見つかった不具合を修正**：この移動作業で、dashboard-grid（`#dashboard-section`）の直後に元から余分な`</div>`が1つ存在し、`.app-container`（`max-width:1600px; margin:0 auto; padding:2rem`を持つ最上位のレイアウトコンテナ）をそこで早期に閉じてしまっていたことが判明。結果として`.app-details`と`<footer>`が`.app-container`の外（`<body>`直下）に置かれ、コンテナの幅制限・余白・flexの`gap`を受けられない状態になっていた（実機のDOM調査で発覚。見た目には大きく破綻していなかったため気づかれていなかった）。余分な`</div>`を削除し、`.app-details`・`<footer>`とも`.app-container`の子として正しくネストされることを確認済み
     - 動作確認：未読込状態・データ読込後の両方で、`.app-container`の子要素の並び順が`header → header-actions → upload-section → dashboard-section → .app-details → footer`になっていること、`.app-details`が`display:none`・`<details>`内のいずれにも該当せず常に表示されることをブラウザ実機で確認済み。`node --check`・`npm test`（105件）はパス。`app.js`は今回も無変更
4. **AdSenseによる広告収益化**：申請・審査（無料。振込は$100貯まってから、非居住者はW-8BEN提出が必要）→プライバシーポリシーへ広告/Cookie利用を追記→（EU圏配慮が必要なら）Cookie同意バナー→広告スクリプトの埋め込み
5. **（任意）ストア配布**：Google Playストア登録（$25買い切り、PWAをTWA化）。Apple App Store（$99/年）は費用対効果が見合わないと判断し見送り

保留中・優先度低のまま変わらず：Google連動カテゴリーのGoogle Places API方式（選択肢a）

<!-- 以下、追加の要望をここに書き足していってください -->
